CERTIFIED AUTHORED-HSON CORPUS — FULLY MATERIALIZED REVIEW

SUMMARY
{
  "counts": {
    "literalAcceptedAuthoredHson": 51,
    "transparentAcceptedAuthoredHson": 50,
    "literalRejectedAuthoredHson": 55,
    "transparentRejectedAuthoredHson": 117,
    "graphOnlyAcceptedTransport": 11,
    "graphOnlyRejectedTransport": 9,
    "structuralJsonTransport": 14,
    "structuralHtmlTransport": 49,
    "diagnosticCircuitRegressions": 4,
    "specializedTestReferences": 10,
    "totalConcreteDescriptors": 370,
    "uniqueAuthoredSources": 273,
    "declaredSourceReuse": 0
  },
  "assertions": {
    "acceptedAssertions": 1089,
    "rejectedAssertions": 1764,
    "integrityAssertions": 24,
    "totalAssertions": 2877
  }
}

TRANSPORT INVENTORY
- graph.accept.element-string-empty
- graph.accept.element-string-ordinary
- graph.accept.object-false
- graph.accept.object-negative-number
- graph.accept.object-negative-zero
- graph.accept.object-null
- graph.accept.object-positive-number
- graph.accept.object-string-empty
- graph.accept.object-string-ordinary
- graph.accept.object-true
- graph.accept.object-zero
- graph.reject.direct-element-false
- graph.reject.direct-element-negative-number
- graph.reject.direct-element-negative-zero
- graph.reject.direct-element-null
- graph.reject.direct-element-positive-number
- graph.reject.direct-element-true
- graph.reject.direct-element-zero
- graph.reject.fragment-typed-child
- graph.reject.nested-element-number
- html.accept.control-empty-unicode-adjacent
- html.accept.empty-nonempty-empty
- html.accept.identical-adjacent
- html.accept.ingress.element-string-empty
- html.accept.ingress.element-string-ordinary
- html.accept.ingress.object-false
- html.accept.ingress.object-negative-number
- html.accept.ingress.object-negative-zero
- html.accept.ingress.object-null
- html.accept.ingress.object-positive-number
- html.accept.ingress.object-string-empty
- html.accept.ingress.object-string-ordinary
- html.accept.ingress.object-true
- html.accept.ingress.object-zero
- html.accept.ordinary-melted-text
- html.accept.script-line-ending-carrier
- html.accept.script-ordinary-raw-text
- html.accept.script-segmented-carrier
- html.accept.string-ampersand
- html.accept.string-backslash
- html.accept.string-backspace
- html.accept.string-bmp-non-ascii
- html.accept.string-carriage-return
- html.accept.string-crlf
- html.accept.string-form-feed
- html.accept.string-greater-than
- html.accept.string-isolated-high-surrogate
- html.accept.string-isolated-low-surrogate
- html.accept.string-less-than
- html.accept.string-line-feed
- html.accept.string-nul
- html.accept.string-quote
- html.accept.string-slash
- html.accept.string-supplementary-unicode
- html.accept.string-tab
- html.accept.style-line-ending-carrier
- html.accept.style-ordinary-raw-text
- html.accept.style-segmented-carrier
- html.accept.three-adjacent-strings
- html.accept.three-empty-strings
- html.accept.two-adjacent-strings
- html.reject.direct-element-false
- html.reject.direct-element-negative-number
- html.reject.direct-element-negative-zero
- html.reject.direct-element-null
- html.reject.direct-element-true
- html.reject.direct-element-value
- html.reject.direct-element-zero
- html.reject.nested-element-value
- json.accept.structural-json-order.array-inside-object
- json.accept.structural-json-order.direct-integer-keys
- json.accept.structural-json-order.mixed-key-classes
- json.accept.structural-json-order.negative-zero
- json.accept.structural-json-order.nested-object
- json.accept.structural-json-order.object-inside-array
- json.accept.unusual-authored-keys
- json.reject.duplicate.adjacent
- json.reject.duplicate.array-contained
- json.reject.duplicate.decoded-equivalent
- json.reject.duplicate.multiline-evidence
- json.reject.duplicate.nested
- json.reject.duplicate.order-coexistence
- json.reject.duplicate.separated

MATERIALIZED CASES
=== diagnostic.divergence-cannot-replace-baseline ===
claim: A divergent intermediate graph cannot replace the diagnostic baseline.
classification: diagnostic-circuit-regression
ingress format: diagnostic
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"value\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[2]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: diagnostic-baseline
variation: divergence-cannot-replace-baseline
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                2
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "value"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transform-test-oracle-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: diagnostic-circuit, divergence
rationale: A divergent intermediate graph cannot replace the diagnostic baseline.

=== diagnostic.lossy-fixed-point-cannot-pass ===
claim: Lossy fixed-point convergence cannot satisfy strict baseline closure.
classification: diagnostic-circuit-regression
ingress format: diagnostic
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"before\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[1]}]}]},{\"$_tag\":\"after\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[2]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: diagnostic-baseline
variation: lossy-fixed-point-cannot-pass
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "before"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                2
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "after"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transform-test-oracle-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: diagnostic-circuit, fixed-point, divergence
rationale: Lossy fixed-point convergence cannot satisfy strict baseline closure.

=== diagnostic.negative-zero-witness ===
claim: Diagnostic witness rendering distinguishes negative zero from zero.
classification: diagnostic-circuit-regression
ingress format: diagnostic
escaped input: "{\"$_tag\":\"_hson_val\",\"$_content\":[0]}"
verbatim input: —
semantic shape: _hson_val
slot: diagnostic-baseline
variation: negative-zero-witness
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$number": "-0"
    }
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: —
exact diagnostic output: "{\n  \"actualClassification\": \"canonical-divergence\",\n  \"case\": \"negative-zero-witness\",\n  \"expectedClassification\": \"success\",\n  \"graphFixture\": {\n    \"$_content\": [\n      {\n        \"$number\": \"-0\"\n      }\n    ],\n    \"$_tag\": \"_hson_val\"\n  },\n  \"launcher\": \"certified-authored-hson-corpus\",\n  \"operation\": \"render\",\n  \"stage\": \"canonical-comparison\"\n}"
expected key sequences: —
expected string leaves: —
negative-zero paths:
[
  "$.$_content[0]"
]
structured rejection: —
origin: transform-test-oracle-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: diagnostic-circuit, negative-zero, witness
rationale: Diagnostic witness rendering distinguishes negative zero from zero.

=== diagnostic.original-baseline-retained ===
claim: Every diagnostic route compares against the original admitted graph baseline.
classification: diagnostic-circuit-regression
ingress format: diagnostic
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"value\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[1]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: diagnostic-baseline
variation: original-baseline-retained
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "value"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transform-test-oracle-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: diagnostic-circuit, baseline
rationale: Every diagnostic route compares against the original admitted graph baseline.

=== graph.accept.element-string-empty ===
claim: A detached HSON element string carrier normalizes to its empty text leaf.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: detached-semantic-boundary
variation: element-string-empty
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\""
exact JSON output: "\"\""
exact HTML output: "<_hson_obj><_hson_str>&quot;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON element string carrier normalizes to its empty text leaf.

=== graph.accept.element-string-ordinary ===
claim: A detached HSON element string carrier normalizes to its ordinary text leaf.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ordinary\"]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: detached-semantic-boundary
variation: element-string-ordinary
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "ordinary"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"ordinary\""
exact JSON output: "\"ordinary\""
exact HTML output: "<_hson_obj><_hson_str>&quot;ordinary&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON element string carrier normalizes to its ordinary text leaf.

=== graph.accept.object-false ===
claim: A detached HSON object carrier transports false.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[false]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-false
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    false
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "false"
exact JSON output: "false"
exact HTML output: "<_hson_obj><_hson_val>false</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object carrier transports false.

=== graph.accept.object-negative-number ===
claim: A detached HSON object carrier transports a negative finite number.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[-12.5]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-negative-number
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    -12.5
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "-12.5"
exact JSON output: "-12.5"
exact HTML output: "<_hson_obj><_hson_val>-12.5</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object carrier transports a negative finite number.

=== graph.accept.object-negative-zero ===
claim: A detached HSON object carrier transports negative zero.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[0]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-negative-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$number": "-0"
    }
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "-0"
exact JSON output: "-0"
exact HTML output: "<_hson_obj><_hson_val>-0</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths:
[
  "$.$_content[0]"
]
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object carrier transports negative zero.

=== graph.accept.object-null ===
claim: A detached HSON object carrier transports null.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[null]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-null
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    null
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "null"
exact JSON output: "null"
exact HTML output: "<_hson_obj><_hson_val>null</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object carrier transports null.

=== graph.accept.object-positive-number ===
claim: A detached HSON object carrier transports a positive finite number.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[12.5]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-positive-number
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    12.5
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "12.5"
exact JSON output: "12.5"
exact HTML output: "<_hson_obj><_hson_val>12.5</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object carrier transports a positive finite number.

=== graph.accept.object-string-empty ===
claim: A detached HSON object string carrier normalizes to its empty string leaf.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-string-empty
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\""
exact JSON output: "\"\""
exact HTML output: "<_hson_obj><_hson_str>&quot;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object string carrier normalizes to its empty string leaf.

=== graph.accept.object-string-ordinary ===
claim: A detached HSON object string carrier normalizes to its ordinary string leaf.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ordinary\"]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-string-ordinary
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "ordinary"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"ordinary\""
exact JSON output: "\"ordinary\""
exact HTML output: "<_hson_obj><_hson_str>&quot;ordinary&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object string carrier normalizes to its ordinary string leaf.

=== graph.accept.object-true ===
claim: A detached HSON object carrier transports true.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[true]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-true
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    true
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "true"
exact JSON output: "true"
exact HTML output: "<_hson_obj><_hson_val>true</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object carrier transports true.

=== graph.accept.object-zero ===
claim: A detached HSON object carrier transports zero.
classification: graph-ingress-accepted-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[0]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: detached-semantic-boundary
variation: object-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    0
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "0"
exact JSON output: "0"
exact HTML output: "<_hson_obj><_hson_val>0</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, scalar-carrier, transport
rationale: A detached HSON object carrier transports zero.

=== graph.reject.direct-element-false ===
claim: A detached HSON element carrier may not contain a typed false leaf.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[false]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: direct-element-false
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: A detached HSON element carrier may not contain a typed false leaf.

=== graph.reject.direct-element-negative-number ===
claim: A detached HSON element carrier may not contain a typed negative numeric leaf.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[-12.5]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: direct-element-negative-number
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: A detached HSON element carrier may not contain a typed negative numeric leaf.

=== graph.reject.direct-element-negative-zero ===
claim: A detached HSON element carrier may not contain a typed negative-zero leaf.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[0]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: direct-element-negative-zero
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: A detached HSON element carrier may not contain a typed negative-zero leaf.

=== graph.reject.direct-element-null ===
claim: A detached HSON element carrier may not contain a typed null leaf.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[null]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: direct-element-null
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: A detached HSON element carrier may not contain a typed null leaf.

=== graph.reject.direct-element-positive-number ===
claim: A detached HSON element carrier may not contain a typed positive numeric leaf.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[12.5]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: direct-element-positive-number
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: A detached HSON element carrier may not contain a typed positive numeric leaf.

=== graph.reject.direct-element-true ===
claim: A detached HSON element carrier may not contain a typed true leaf.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[true]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: direct-element-true
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: A detached HSON element carrier may not contain a typed true leaf.

=== graph.reject.direct-element-zero ===
claim: A detached HSON element carrier may not contain a typed zero leaf.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[0]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: direct-element-zero
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: A detached HSON element carrier may not contain a typed zero leaf.

=== graph.reject.fragment-typed-child ===
claim: An element fragment containing one typed child rejects as a whole.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"a\",\"$_content\":[]},{\"$_tag\":\"_hson_val\",\"$_content\":[false]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: fragment-typed-child
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: An element fragment containing one typed child rejects as a whole.

=== graph.reject.nested-element-number ===
claim: An ordinary element may not retain a typed numeric leaf beneath its element content.
classification: graph-ingress-rejected-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[1]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: element-content
variation: —
defect: nested-element-number
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "fromNode",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: graph-ingress, typed-value, structural-mode, rejection
rationale: An ordinary element may not retain a typed numeric leaf beneath its element content.

=== hson.accept.basis.number.negative-exponent-sign ===
claim: An exponent may contain a minus sign.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"1e-3\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: negative-exponent-sign
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    0.001
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "0.001"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the negative exponent-sign spelling.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, exponent
rationale: An exponent may contain a minus sign.

=== hson.accept.basis.number.negative-integer ===
claim: A minus may prefix a nonzero JSON integer.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"-1\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: negative-integer
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    -1
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "-1"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the missing negative-integer lexical branch.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar
rationale: A minus may prefix a nonzero JSON integer.

=== hson.accept.basis.number.positive-exponent-sign ===
claim: An exponent may contain an explicit plus sign.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"1e+3\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: positive-exponent-sign
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    1000
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "1000"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the positive exponent-sign spelling.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, exponent
rationale: An exponent may contain an explicit plus sign.

=== hson.accept.basis.number.positive-fraction ===
claim: A fraction retains digits on both sides of the decimal point.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"0.5\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: positive-fraction
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    0.5
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "0.5"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the missing positive-fraction lexical branch.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar
rationale: A fraction retains digits on both sides of the decimal point.

=== hson.accept.basis.number.uppercase-exponent ===
claim: An uppercase exponent marker is accepted.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"1E3\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: uppercase-exponent
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    1000
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "1000"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the uppercase exponent-marker spelling.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, exponent
rationale: An uppercase exponent marker is accepted.

=== hson.accept.basis.object.primitive-looking-keys ===
claim: true, false, and null are ordinary property keys in HSON object key position.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<true 1 false 2 null 3>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: primitive-looking-keys
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "true"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                2
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "false"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                3
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "null"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<\n  true 1\n  false 2\n  null 3\n>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled authored-HSON structural-mode contract.
novelty: Adds the contextual-key contrast with typed object values and element flags.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: object, property-key, primitive-keyword, order
rationale: true, false, and null are ordinary property keys in HSON object key position.

=== hson.accept.basis.quoted-name.element-name ===
claim: A nonempty single-quoted name is admitted as an HSON element name.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<'x y'/>\""
verbatim input: —
semantic shape: element
slot: element-name
variation: element-name
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [],
      "$_tag": "x y"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<'x y'/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription after auditing the existing authored-name role grammar.
novelty: Adds the missing positive quoted element-name role.
human-review priority: high
specialized-test reference: —
declared source reuse: —
tags: element, quoted-name, authored-name
rationale: A nonempty single-quoted name is admitted as an HSON element name.

=== hson.accept.basis.root.element-fragment ===
claim: A homogeneous root element fragment preserves sibling order.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<a/><b/>\""
verbatim input: —
semantic shape: element-fragment
slot: root
variation: element-fragment
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [],
      "$_tag": "a"
    },
    {
      "$_content": [],
      "$_tag": "b"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<a/>\n<b/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled authored-HSON structural-mode contract.
novelty: Adds the missing positive multi-element root fragment.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: root-boundary, element, fragment, order
rationale: A homogeneous root element fragment preserves sibling order.

=== hson.accept.basis.trivia.array-slots ===
claim: Array trivia covers after-opener, before-comma, after-comma, before-closer, and a terminated item-boundary comment.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"[ \\t1 \\r,\\n// next item\\n 2 \\r\\n]\""
verbatim input: "[ \t1 \r,\n// next item\n 2 \r\n]"
semantic shape: array
slot: array-trivia-slots
variation: array-slots
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            1
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            2
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "1"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: "«\n  1,\n  2\n»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Newly reasoned from the settled SPACE/HT/LF/CR and physical-line-comment grammar.
novelty: Adds one readable basis spanning the missing array trivia slots.
human-review priority: high
specialized-test reference: —
declared source reuse: —
tags: trivia, comment, array, crlf
rationale: Array trivia covers after-opener, before-comma, after-comma, before-closer, and a terminated item-boundary comment.

=== hson.accept.basis.trivia.comment-to-eof ===
claim: A physical-line comment may supply trailing root trivia through EOF.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"42// comment to EOF\""
verbatim input: —
semantic shape: scalar
slot: source-trailing-trivia
variation: comment-to-eof
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    42
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "42"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Newly reasoned from the settled SPACE/HT/LF/CR and physical-line-comment grammar.
novelty: Adds the missing valid comment-to-EOF source boundary.
human-review priority: high
specialized-test reference: —
declared source reuse: —
tags: trivia, comment, eof
rationale: A physical-line comment may supply trailing root trivia through EOF.

=== hson.accept.basis.trivia.element-slots ===
claim: Element trivia covers before-name, after-name, around equals, between header items, before and between content, and before slash.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"< \\nwidget \\t title \\r= \\n\\\"value\\\"\\r\\n enabled \\t \\\"a\\\"// next leaf\\n \\\"b\\\" \\r />\""
verbatim input: "< \nwidget \t title \r= \n\"value\"\r\n enabled \t \"a\"// next leaf\n \"b\" \r />"
semantic shape: element
slot: element-trivia-slots
variation: element-slots
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_attrs": {
        "enabled": "enabled",
        "title": "value"
      },
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "b"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "widget"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<widget title=\"value\" enabled\n  \"a\"\n  \"b\"\n/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: The graph was newly reasoned from the settled trivia grammar; the exact canonical HSON attribute order was corrected after focused production validation.
novelty: Adds one readable basis spanning the missing element trivia slots while leaving '/>' indivisible.
human-review priority: critical
specialized-test reference: —
declared source reuse: —
tags: trivia, comment, element, attribute, flag, crlf, implementation-derived-output
rationale: Element trivia covers before-name, after-name, around equals, between header items, before and between content, and before slash.

=== hson.accept.basis.trivia.object-slots ===
claim: Object trivia covers after-open, key/value, sibling, before-close, and consecutive terminated-comment slots.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"< \\talpha// first\\n// second\\r\\n 1\\r beta\\t2 \\n>\""
verbatim input: "< \talpha// first\n// second\r\n 1\r beta\t2 \n>"
semantic shape: object
slot: object-trivia-slots
variation: object-slots
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "alpha"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                2
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "beta"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<\n  alpha 1\n  beta 2\n>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: targeted-authored-hson-completeness-amendment
provenance: Newly reasoned from the settled SPACE/HT/LF/CR and physical-line-comment grammar.
novelty: Adds one readable basis spanning the missing object trivia slots and consecutive comments.
human-review priority: high
specialized-test reference: —
declared source reuse: —
tags: trivia, comment, object, crlf
rationale: Object trivia covers after-open, key/value, sibling, before-close, and consecutive terminated-comment slots.

=== hson.accept.family.quoted-name.backspace ===
claim: Quoted-name escape variation backspace decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'back\\\\bspace' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: backspace
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "back\bspace"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'back\\bspace' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.carriage-return ===
claim: Quoted-name escape variation carriage-return decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'line\\\\rname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: carriage-return
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "line\rname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'line\\rname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.consecutive-unicode ===
claim: Quoted-name escape variation consecutive-unicode decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'pair\\\\u0041\\\\u0042name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: consecutive-unicode
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "pairABname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<pairABname 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.escaped-apostrophe ===
claim: Quoted-name escape variation escaped-apostrophe decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'don\\\\'t' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: escaped-apostrophe
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "don't"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'don\\'t' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.escaped-backslash ===
claim: Quoted-name escape variation escaped-backslash decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'back\\\\\\\\slash' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: escaped-backslash
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "back\\slash"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'back\\\\slash' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.form-feed ===
claim: Quoted-name escape variation form-feed decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'form\\\\ffeed' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: form-feed
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "form\ffeed"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'form\\ffeed' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.line-feed ===
claim: Quoted-name escape variation line-feed decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'line\\\\nname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: line-feed
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "line\nname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'line\\nname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.literal-backtick ===
claim: Quoted-name escape variation literal-backtick decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'tick`name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: literal-backtick
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "tick`name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'tick`name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.tab ===
claim: Quoted-name escape variation tab decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'line\\\\tname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: tab
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "line\tname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'line\\tname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-control ===
claim: Quoted-name escape variation unicode-control decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'control\\\\u0001name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-control
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "control\u0001name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'control\\u0001name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-high-surrogate ===
claim: Quoted-name escape variation unicode-high-surrogate decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'high\\\\uD800name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-high-surrogate
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "high\ud800name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'high\ud800name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-lambda ===
claim: Quoted-name escape variation unicode-lambda decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'lambda\\\\u03bbname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-lambda
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "lambdaλname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'lambdaλname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-low-surrogate ===
claim: Quoted-name escape variation unicode-low-surrogate decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'low\\\\uDC00name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-low-surrogate
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "low\udc00name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'low\udc00name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-lowercase ===
claim: Quoted-name escape variation unicode-lowercase decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'lower\\\\u0061name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-lowercase
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "loweraname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<loweraname 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-mixed-case ===
claim: Quoted-name escape variation unicode-mixed-case decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'mixed\\\\u00aFname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-mixed-case
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "mixed¯name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'mixed¯name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-surrogate-pair ===
claim: Quoted-name escape variation unicode-surrogate-pair decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'pair\\\\uD83D\\\\uDE00name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-surrogate-pair
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "pair😀name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'pair😀name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u0000 ===
claim: Quoted-name escape variation unicode-u0000 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'nul\\\\u0000name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u0000
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "nul\u0000name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'nul\\u0000name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u001f ===
claim: Quoted-name escape variation unicode-u001f decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'unit\\\\u001Fname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u001f
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "unit\u001fname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'unit\\u001fname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u007f ===
claim: Quoted-name escape variation unicode-u007f decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'unit\\\\u007fname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u007f
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "unitname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'unit\u007fname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u0080 ===
claim: Quoted-name escape variation unicode-u0080 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'unit\\\\u0080name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u0080
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "unitname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'unit\u0080name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u00ff ===
claim: Quoted-name escape variation unicode-u00ff decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'unit\\\\u00FFname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u00ff
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "unitÿname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'unitÿname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u0100 ===
claim: Quoted-name escape variation unicode-u0100 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'unit\\\\u0100name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u0100
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "unitĀname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'unitĀname' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u2028 ===
claim: Quoted-name escape variation unicode-u2028 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'unit\\\\u2028name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u2028
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "unit name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'unit\u2028name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-u2029 ===
claim: Quoted-name escape variation unicode-u2029 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'unit\\\\u2029name' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-u2029
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "unit name"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'unit\u2029name' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-name.unicode-uppercase ===
claim: Quoted-name escape variation unicode-uppercase decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<'upper\\\\u006Aname' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: unicode-uppercase
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "upperjname"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<upperjname 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-name-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family
rationale: One property-key slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.backslash ===
claim: Quoted-string escape variation backslash decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\\\\\\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: backslash
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\\"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\\\\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.backspace ===
claim: Quoted-string escape variation backspace decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\b\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: backspace
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\b"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\b\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.carriage-return ===
claim: Quoted-string escape variation carriage-return decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\r\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: carriage-return
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\r"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\r\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.consecutive-unicode ===
claim: Quoted-string escape variation consecutive-unicode decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u0041\\\\u0042\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: consecutive-unicode
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "AB"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"AB\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.escape-before-container-closer ===
claim: A valid escape may end immediately before an element closer.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"<e \\\"x\\\\t\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-content
variation: escape-before-container-closer
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "x\t"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "e"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<e \"x\\t\"/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: This materializes the grammar-boundary interaction without replaying the escape lattice.

=== hson.accept.family.quoted-string.escape-before-quote ===
claim: Quoted-string escape variation escape-before-quote decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"end\\\\t\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: escape-before-quote
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "end\t"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"end\\t\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.form-feed ===
claim: Quoted-string escape variation form-feed decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\f\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: form-feed
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\f"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\f\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.line-feed ===
claim: Quoted-string escape variation line-feed decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\n\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: line-feed
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\n"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\n\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.quote ===
claim: Quoted-string escape variation quote decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\\\\"\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: quote
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\\"\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.slash ===
claim: Quoted-string escape variation slash decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\/\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: slash
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "/"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"/\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.tab ===
claim: Quoted-string escape variation tab decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\t\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: tab
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\t"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\t\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-high-surrogate ===
claim: Quoted-string escape variation unicode-high-surrogate decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\uD800\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-high-surrogate
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\ud800"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\ud800\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-low-surrogate ===
claim: Quoted-string escape variation unicode-low-surrogate decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\uDC00\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-low-surrogate
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\udc00"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\udc00\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-lowercase ===
claim: Quoted-string escape variation unicode-lowercase decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u0061\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-lowercase
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "a"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"a\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-mixed-case ===
claim: Quoted-string escape variation unicode-mixed-case decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u00aF\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-mixed-case
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "¯"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"¯\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-surrogate-pair ===
claim: Quoted-string escape variation unicode-surrogate-pair decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\uD83D\\\\uDE00\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-surrogate-pair
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "😀"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"😀\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u0000 ===
claim: Quoted-string escape variation unicode-u0000 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u0000\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u0000
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\u0000"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\u0000\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u001f ===
claim: Quoted-string escape variation unicode-u001f decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u001F\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u001f
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\u001f"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\\u001f\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u007f ===
claim: Quoted-string escape variation unicode-u007f decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u007F\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u007f
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\u007f\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u0080 ===
claim: Quoted-string escape variation unicode-u0080 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u0080\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u0080
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\u0080\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u00ff ===
claim: Quoted-string escape variation unicode-u00ff decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u00FF\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u00ff
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "ÿ"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"ÿ\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u0100 ===
claim: Quoted-string escape variation unicode-u0100 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u0100\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u0100
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "Ā"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"Ā\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u2028 ===
claim: Quoted-string escape variation unicode-u2028 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u2028\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u2028
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    " "
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\u2028\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-u2029 ===
claim: Quoted-string escape variation unicode-u2029 decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u2029\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-u2029
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    " "
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\u2029\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.family.quoted-string.unicode-uppercase ===
claim: Quoted-string escape variation unicode-uppercase decodes and canonicalizes exactly.
classification: materialized-accepted-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u006A\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: unicode-uppercase
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "j"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"j\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: family.accept.quoted-string-json-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: string, escape, transparent-family
rationale: One lexical slot varies only the accepted escape spelling.

=== hson.accept.literal.array.empty-bracket ===
claim: An empty bracket array canonicalizes to guillemets.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"[]\""
verbatim input: —
semantic shape: array
slot: root-array
variation: empty-bracket
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [],
  "$_tag": "_hson_arr"
}
exact HSON output: "«»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, variation
rationale: An empty bracket array canonicalizes to guillemets.

=== hson.accept.literal.array.empty-guillemet ===
claim: An empty guillemet array admits.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"«»\""
verbatim input: —
semantic shape: array
slot: root-array
variation: empty-guillemet
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [],
  "$_tag": "_hson_arr"
}
exact HSON output: "«»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array
rationale: An empty guillemet array admits.

=== hson.accept.literal.array.negative-zero ===
claim: An array item preserves negative zero.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"[-0,0]\""
verbatim input: —
semantic shape: array
slot: array-item
variation: negative-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$number": "-0"
            }
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            0
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "1"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: "«\n  -0,\n  0\n»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths:
[
  "$.$_content[0].$_content[0].$_content[0]"
]
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, negative-zero
rationale: An array item preserves negative zero.

=== hson.accept.literal.array.nested ===
claim: Nested arrays retain indexed membership.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"[[1],«2,3»]\""
verbatim input: —
semantic shape: nested-array
slot: array-item
variation: nested
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    1
                  ],
                  "$_tag": "_hson_val"
                }
              ],
              "$_meta": {
                "index": "0"
              },
              "$_tag": "_hson_ii"
            }
          ],
          "$_tag": "_hson_arr"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    2
                  ],
                  "$_tag": "_hson_val"
                }
              ],
              "$_meta": {
                "index": "0"
              },
              "$_tag": "_hson_ii"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    3
                  ],
                  "$_tag": "_hson_val"
                }
              ],
              "$_meta": {
                "index": "1"
              },
              "$_tag": "_hson_ii"
            }
          ],
          "$_tag": "_hson_arr"
        }
      ],
      "$_meta": {
        "index": "1"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: "«\n  «\n    1\n  »,\n  «\n    2,\n    3\n  »\n»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, nested
rationale: Nested arrays retain indexed membership.

=== hson.accept.literal.array.object-item ===
claim: An array item may be a HSON object.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"[<name \\\"Ada\\\">]\""
verbatim input: —
semantic shape: array-of-object
slot: array-item
variation: object-item
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "Ada"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "name"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: "«\n  <name \"Ada\">\n»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, object
rationale: An array item may be a HSON object.

=== hson.accept.literal.array.primitives ===
claim: Arrays remain comma-separated and retain primitive item order.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"[1,\\\"two\\\",false,null]\""
verbatim input: —
semantic shape: array
slot: array-item
variation: primitives
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            1
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            "two"
          ],
          "$_tag": "_hson_str"
        }
      ],
      "$_meta": {
        "index": "1"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            false
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "2"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            null
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "3"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: "«\n  1,\n  \"two\",\n  false,\n  null\n»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, primitive
rationale: Arrays remain comma-separated and retain primitive item order.

=== hson.accept.literal.array.trailing-comma-bracket ===
claim: A bracket-array trailing comma is accepted variation.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"[1,2,]\""
verbatim input: —
semantic shape: array
slot: trailing-comma
variation: trailing-comma-bracket
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            1
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            2
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "1"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: "«\n  1,\n  2\n»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, variation
rationale: A bracket-array trailing comma is accepted variation.

=== hson.accept.literal.array.trailing-comma-guillemet ===
claim: A guillemet-array trailing comma is accepted variation.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"«1,2,»\""
verbatim input: —
semantic shape: array
slot: trailing-comma
variation: trailing-comma-guillemet
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            1
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            2
          ],
          "$_tag": "_hson_val"
        }
      ],
      "$_meta": {
        "index": "1"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: "«\n  1,\n  2\n»"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, variation
rationale: A guillemet-array trailing comma is accepted variation.

=== hson.accept.literal.element.adjacent-strings ===
claim: Two adjacent authored string leaves remain distinct.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<div \\\"a\\\" \\\"b\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-content
variation: adjacent-strings
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "b"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<div\n  \"a\"\n  \"b\"\n/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, segmentation
rationale: Two adjacent authored string leaves remain distinct.

=== hson.accept.literal.element.attribute ===
claim: An element attribute retains its explicit value.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<input title=\\\"value\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-attribute
variation: attribute
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_attrs": {
        "title": "value"
      },
      "$_content": [],
      "$_tag": "input"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<input title=\"value\"/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, attribute
rationale: An element attribute retains its explicit value.

=== hson.accept.literal.element.empty ===
claim: A self-closing angle construct denotes an empty HSON element.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<div/>\""
verbatim input: —
semantic shape: element
slot: element-name
variation: empty
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<div/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element
rationale: A self-closing angle construct denotes an empty HSON element.

=== hson.accept.literal.element.flag ===
claim: A bare name in the element attribute region is a flag.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<input disabled/>\""
verbatim input: —
semantic shape: element
slot: element-flag
variation: flag
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_attrs": {
        "disabled": "disabled"
      },
      "$_content": [],
      "$_tag": "input"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<input disabled/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, flag
rationale: A bare name in the element attribute region is a flag.

=== hson.accept.literal.element.keyword-flags ===
claim: Bare true, false, and null in the element attribute region are flags.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<x true false null/>\""
verbatim input: —
semantic shape: element
slot: element-flag
variation: keyword-flags
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_attrs": {
        "false": "false",
        "null": "null",
        "true": "true"
      },
      "$_content": [],
      "$_tag": "x"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<x false null true/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, flag, primitive-keyword
rationale: Bare true, false, and null in the element attribute region are flags.

=== hson.accept.literal.element.mixed-content ===
claim: Element strings and nested elements retain their order.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<p \\\"first\\\" <em \\\"middle\\\"/> \\\"last\\\"/>\""
verbatim input: —
semantic shape: nested-element
slot: element-content
variation: mixed-content
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "first"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "middle"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_elem"
                }
              ],
              "$_tag": "em"
            },
            {
              "$_content": [
                "last"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "p"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<p\n  \"first\"\n  <em \"middle\"/>\n  \"last\"\n/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, order
rationale: Element strings and nested elements retain their order.

=== hson.accept.literal.element.nested ===
claim: An HSON element may contain a nested element.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<p <em \\\"text\\\"/>/>\""
verbatim input: —
semantic shape: nested-element
slot: element-content
variation: nested
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "text"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_elem"
                }
              ],
              "$_tag": "em"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "p"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<p\n  <em \"text\"/>\n/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, nested
rationale: An HSON element may contain a nested element.

=== hson.accept.literal.element.quid ===
claim: An element QUID remains supported.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<main @000000001/>\""
verbatim input: —
semantic shape: element
slot: element-quid
variation: quid
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [],
      "$_meta": {
        "quid": "000000001"
      },
      "$_tag": "main"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<main @000000001/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, quid
rationale: An element QUID remains supported.

=== hson.accept.literal.element.text ===
claim: An HSON element may contain quoted string content.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<p \\\"text\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-content
variation: text
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "text"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "p"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<p \"text\"/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, string
rationale: An HSON element may contain quoted string content.

=== hson.accept.literal.element.three-empty-strings ===
claim: Three adjacent empty authored string leaves remain three.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<div \\\"\\\"\\\"\\\"\\\"\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-content
variation: three-empty-strings
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: "<div\n  \"\"\n  \"\"\n  \"\"\n/>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, segmentation, empty-string
rationale: Three adjacent empty authored string leaves remain three.

=== hson.accept.literal.object.array-value ===
claim: An object property may contain an array.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<items «1,2»>\""
verbatim input: —
semantic shape: object-with-array
slot: property-value
variation: array-value
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    1
                  ],
                  "$_tag": "_hson_val"
                }
              ],
              "$_meta": {
                "index": "0"
              },
              "$_tag": "_hson_ii"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    2
                  ],
                  "$_tag": "_hson_val"
                }
              ],
              "$_meta": {
                "index": "1"
              },
              "$_tag": "_hson_ii"
            }
          ],
          "$_tag": "_hson_arr"
        }
      ],
      "$_tag": "items"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<\n  items «\n    1,\n    2\n  »\n>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, array
rationale: An object property may contain an array.

=== hson.accept.literal.object.colon-dot-names ===
claim: Colon and dot keys canonicalize through single-quoted names.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<:x 1 a.b 2>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: colon-dot-names
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": ":x"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                2
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "a.b"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<\n  ':x' 1\n  'a.b' 2\n>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, quoted-name
rationale: Colon and dot keys canonicalize through single-quoted names.

=== hson.accept.literal.object.comments ===
claim: Physical-line comments are grammar trivia between object tokens.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<a// key/value\\n 1 b// key/value\\r\\n 2>\""
verbatim input: "<a// key/value\n 1 b// key/value\r\n 2>"
semantic shape: object
slot: trivia
variation: comments
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "a"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                2
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "b"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<\n  a 1\n  b 2\n>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, comment, crlf
rationale: Physical-line comments are grammar trivia between object tokens.

=== hson.accept.literal.object.empty ===
claim: One angle pair denotes an empty HSON object.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<>\""
verbatim input: —
semantic shape: object
slot: root-object
variation: empty
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [],
  "$_tag": "_hson_obj"
}
exact HSON output: "<>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object
rationale: One angle pair denotes an empty HSON object.

=== hson.accept.literal.object.empty-decoded-key ===
claim: An empty decoded object-property key is valid.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<'' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: empty-decoded-key
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": ""
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<'' 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, quoted-name, empty-name
rationale: An empty decoded object-property key is valid.

=== hson.accept.literal.object.multiple-properties ===
claim: Object property order is retained.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<a 1 b \\\"two\\\" c false>\""
verbatim input: —
semantic shape: object
slot: object-property
variation: multiple-properties
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "a"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "two"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "b"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                false
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "c"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<\n  a 1\n  b \"two\"\n  c false\n>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, order
rationale: Object property order is retained.

=== hson.accept.literal.object.negative-zero ===
claim: An object property preserves negative zero.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<value -0>\""
verbatim input: —
semantic shape: object
slot: property-value
variation: negative-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$number": "-0"
                }
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "value"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<value -0>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths:
[
  "$.$_content[0].$_content[0].$_content[0].$_content[0]"
]
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, negative-zero
rationale: An object property preserves negative zero.

=== hson.accept.literal.object.nested ===
claim: An object property may contain a nested HSON object.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<record <field 2>>\""
verbatim input: —
semantic shape: nested-object
slot: property-value
variation: nested
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        2
                      ],
                      "$_tag": "_hson_val"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "field"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "record"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<record <field 2>>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, nested
rationale: An object property may contain a nested HSON object.

=== hson.accept.literal.object.one-property ===
claim: An object contains a punctuation-free key/value property.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<a 1>\""
verbatim input: —
semantic shape: object
slot: object-property
variation: one-property
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                1
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "a"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<a 1>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object
rationale: An object contains a punctuation-free key/value property.

=== hson.accept.literal.object.typed-keywords ===
claim: true, false, and null in object value position remain typed.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"<t true f false n null>\""
verbatim input: —
semantic shape: object
slot: property-value
variation: typed-keywords
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                true
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "t"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                false
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "f"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                null
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "n"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: "<\n  t true\n  f false\n  n null\n>"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, primitive
rationale: true, false, and null in object value position remain typed.

=== hson.accept.literal.primitive.empty-string ===
claim: A bare empty quoted string admits one string leaf.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"\\\"\\\"\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: empty-string
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, string
rationale: A bare empty quoted string admits one string leaf.

=== hson.accept.literal.primitive.exponent ===
claim: Exponent notation admits and canonicalizes by value.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"1e3\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: exponent
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    1000
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "1000"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, number, variation
rationale: Exponent notation admits and canonicalizes by value.

=== hson.accept.literal.primitive.false ===
claim: Bare false is a typed primitive value.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"false\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: false
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    false
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "false"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, boolean
rationale: Bare false is a typed primitive value.

=== hson.accept.literal.primitive.negative-fraction ===
claim: A negative finite fraction admits.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"-12.5\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: negative-fraction
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    -12.5
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "-12.5"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, number
rationale: A negative finite fraction admits.

=== hson.accept.literal.primitive.negative-zero ===
claim: Negative zero retains exact numeric identity.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"-0\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: negative-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$number": "-0"
    }
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "-0"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths:
[
  "$.$_content[0]"
]
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, number, negative-zero
rationale: Negative zero retains exact numeric identity.

=== hson.accept.literal.primitive.null ===
claim: Bare null is a typed primitive value.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"null\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: null
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    null
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "null"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, null
rationale: Bare null is a typed primitive value.

=== hson.accept.literal.primitive.positive-integer ===
claim: A positive finite integer admits.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"42\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: positive-integer
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    42
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "42"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, number
rationale: A positive finite integer admits.

=== hson.accept.literal.primitive.string ===
claim: A bare ordinary quoted string admits one string leaf.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"\\\"hello\\\"\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: string
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "hello"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: "\"hello\""
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, string
rationale: A bare ordinary quoted string admits one string leaf.

=== hson.accept.literal.primitive.true ===
claim: Bare true is a typed primitive value.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"true\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: true
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    true
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "true"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, boolean
rationale: Bare true is a typed primitive value.

=== hson.accept.literal.primitive.zero ===
claim: Zero admits as a typed numeric leaf.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\"0\""
verbatim input: —
semantic shape: scalar
slot: root-value
variation: zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    0
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "0"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: primitive, number
rationale: Zero admits as a typed numeric leaf.

=== hson.accept.literal.trivia.space-tab-lf-cr ===
claim: Grammar trivia is exactly SPACE, HT, LF, and CR.
classification: literal-accepted-authored-hson
ingress format: hson
escaped input: "\" \\t\\r\\n42\\r\\n\""
verbatim input: " \t\r\n42\r\n"
semantic shape: scalar
slot: trivia
variation: space-tab-lf-cr
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    42
  ],
  "$_tag": "_hson_val"
}
exact HSON output: "42"
exact JSON output: —
exact HTML output: —
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, whitespace
rationale: Grammar trivia is exactly SPACE, HT, LF, and CR.

=== hson.reject.basis.mode.object-element ===
claim: An HSON object property cannot contain an element-mode value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a <e/>>\""
verbatim input: —
semantic shape: object
slot: property-value
variation: —
defect: element-beneath-object
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRUCTURAL_MODE_CROSSING",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled authored-HSON structural-mode contract.
novelty: Adds the previously missing direct object-to-element crossing.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: structural-mode, object, element, rejection
rationale: An HSON object property cannot contain an element-mode value.

=== hson.reject.basis.number.hexadecimal ===
claim: Hexadecimal spelling is not JSON-compatible HSON number syntax.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"0x10\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: unsupported-hexadecimal
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_UNSUPPORTED_SPELLING",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds explicit hexadecimal-spelling rejection.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: Hexadecimal spelling is not JSON-compatible HSON number syntax.

=== hson.reject.basis.number.leading-plus ===
claim: A JSON-compatible HSON number may not begin with plus.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"+1\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: leading-plus
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_LEADING_PLUS",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds direct ownership for the leading-plus defect.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: A JSON-compatible HSON number may not begin with plus.

=== hson.reject.basis.number.leading-zero ===
claim: A nonzero integer may not begin with zero.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"01\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: leading-zero
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_LEADING_ZERO",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds direct ownership for the leading-zero defect.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: A nonzero integer may not begin with zero.

=== hson.reject.basis.number.missing-exponent-digits ===
claim: An exponent marker requires following digits.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"1e\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: missing-exponent-digits
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_INCOMPLETE_EXPONENT",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the empty exponent boundary.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: An exponent marker requires following digits.

=== hson.reject.basis.number.missing-fraction-digits ===
claim: A decimal point requires following fraction digits.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"1.\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: missing-fraction-digits
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_INCOMPLETE_FRACTION",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the missing fraction-digits boundary.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: A decimal point requires following fraction digits.

=== hson.reject.basis.number.missing-integer-before-fraction ===
claim: A fraction requires an integer component before the decimal point.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\".5\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: missing-integer-before-fraction
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_INCOMPLETE_FRACTION",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the missing-integer fraction boundary.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: A fraction requires an integer component before the decimal point.

=== hson.reject.basis.number.missing-signed-exponent-digits ===
claim: An exponent sign requires following digits.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"1e+\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: missing-signed-exponent-digits
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_INCOMPLETE_EXPONENT",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the signed empty exponent boundary.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: An exponent sign requires following digits.

=== hson.reject.basis.number.named-nan ===
claim: NaN is not an authored finite number.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"NaN\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: named-nonfinite
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_UNSUPPORTED_SPELLING",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds explicit named-NaN rejection.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, nonfinite, rejection
rationale: NaN is not an authored finite number.

=== hson.reject.basis.number.named-negative-infinity ===
claim: Negative Infinity is not an authored finite number.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"-Infinity\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: named-nonfinite
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_UNSUPPORTED_SPELLING",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds explicit negative-Infinity rejection.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, nonfinite, rejection
rationale: Negative Infinity is not an authored finite number.

=== hson.reject.basis.number.named-positive-infinity ===
claim: Infinity is not an authored finite number.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"Infinity\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: named-nonfinite
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_UNSUPPORTED_SPELLING",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds explicit positive-Infinity rejection.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, nonfinite, rejection
rationale: Infinity is not an authored finite number.

=== hson.reject.basis.number.nonfinite-overflow ===
claim: A syntactically valid number that overflows binary64 rejects as nonfinite.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"1e309\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: nonfinite-overflow
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_NONFINITE",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds the finite-domain overflow boundary.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, nonfinite, rejection
rationale: A syntactically valid number that overflows binary64 rejects as nonfinite.

=== hson.reject.basis.number.numeric-separator ===
claim: Numeric separators are not JSON-compatible HSON number syntax.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"1_0\""
verbatim input: —
semantic shape: scalar
slot: root-number
variation: —
defect: unsupported-separator
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_UNSUPPORTED_SPELLING",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription of the settled JSON-compatible finite-number grammar.
novelty: Adds explicit numeric-separator rejection.
human-review priority: medium
specialized-test reference: —
declared source reuse: —
tags: number, json-number-grammar, rejection
rationale: Numeric separators are not JSON-compatible HSON number syntax.

=== hson.reject.basis.quoted-name.attribute-name ===
claim: Single-quoted names are not admitted as element attribute names.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e 'data key'=\\\"value\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-attribute
variation: —
defect: quoted-name-role-forbidden
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_INVALID_START",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription after auditing the existing authored-name role grammar.
novelty: Adds the missing nonempty quoted-name attribute-role rejection.
human-review priority: high
specialized-test reference: —
declared source reuse: —
tags: element, attribute, quoted-name, rejection
rationale: Single-quoted names are not admitted as element attribute names.

=== hson.reject.basis.quoted-name.flag-name ===
claim: Single-quoted names are not admitted as element flag names.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e 'feature flag'/>\""
verbatim input: —
semantic shape: element
slot: element-flag
variation: —
defect: quoted-name-role-forbidden
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_INVALID_START",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: targeted-authored-hson-completeness-amendment
provenance: Direct transcription after auditing the existing authored-name role grammar.
novelty: Adds the missing nonempty quoted-name flag-role rejection.
human-review priority: high
specialized-test reference: —
declared source reuse: —
tags: element, flag, quoted-name, rejection
rationale: Single-quoted names are not admitted as element flag names.

=== hson.reject.family.quoted-name.eof ===
claim: quoted-name defect eof rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'name\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: eof
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_UNTERMINATED",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.invalid-hex-position-1 ===
claim: quoted-name defect invalid-hex-position-1 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\uG000' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: invalid-hex-position-1
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.invalid-hex-position-2 ===
claim: quoted-name defect invalid-hex-position-2 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u0G00' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: invalid-hex-position-2
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.invalid-hex-position-3 ===
claim: quoted-name defect invalid-hex-position-3 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u00G0' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: invalid-hex-position-3
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.invalid-hex-position-4 ===
claim: quoted-name defect invalid-hex-position-4 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u000G' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: invalid-hex-position-4
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.raw-u0000 ===
claim: Raw U+0000 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0000b' 1>"
verbatim input: "<'a\u0000b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0000
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0001 ===
claim: Raw U+0001 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0001b' 1>"
verbatim input: "<'a\u0001b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0001
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0002 ===
claim: Raw U+0002 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0002b' 1>"
verbatim input: "<'a\u0002b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0002
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0003 ===
claim: Raw U+0003 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0003b' 1>"
verbatim input: "<'a\u0003b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0003
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0004 ===
claim: Raw U+0004 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0004b' 1>"
verbatim input: "<'a\u0004b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0004
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0005 ===
claim: Raw U+0005 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0005b' 1>"
verbatim input: "<'a\u0005b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0005
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0006 ===
claim: Raw U+0006 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0006b' 1>"
verbatim input: "<'a\u0006b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0006
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0007 ===
claim: Raw U+0007 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0007b' 1>"
verbatim input: "<'a\u0007b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0007
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0008 ===
claim: Raw U+0008 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0008b' 1>"
verbatim input: "<'a\bb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0008
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0009 ===
claim: Raw U+0009 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0009b' 1>"
verbatim input: "<'a\tb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0009
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u000a ===
claim: Raw U+000A rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u000ab' 1>"
verbatim input: "<'a\nb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u000a
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u000b ===
claim: Raw U+000B rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u000bb' 1>"
verbatim input: "<'a\u000bb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u000b
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u000c ===
claim: Raw U+000C rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u000cb' 1>"
verbatim input: "<'a\fb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u000c
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u000d ===
claim: Raw U+000D rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u000db' 1>"
verbatim input: "<'a\rb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u000d
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u000e ===
claim: Raw U+000E rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u000eb' 1>"
verbatim input: "<'a\u000eb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u000e
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u000f ===
claim: Raw U+000F rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u000fb' 1>"
verbatim input: "<'a\u000fb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u000f
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0010 ===
claim: Raw U+0010 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0010b' 1>"
verbatim input: "<'a\u0010b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0010
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0011 ===
claim: Raw U+0011 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0011b' 1>"
verbatim input: "<'a\u0011b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0011
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0012 ===
claim: Raw U+0012 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0012b' 1>"
verbatim input: "<'a\u0012b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0012
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0013 ===
claim: Raw U+0013 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0013b' 1>"
verbatim input: "<'a\u0013b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0013
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0014 ===
claim: Raw U+0014 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0014b' 1>"
verbatim input: "<'a\u0014b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0014
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0015 ===
claim: Raw U+0015 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0015b' 1>"
verbatim input: "<'a\u0015b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0015
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0016 ===
claim: Raw U+0016 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0016b' 1>"
verbatim input: "<'a\u0016b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0016
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0017 ===
claim: Raw U+0017 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0017b' 1>"
verbatim input: "<'a\u0017b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0017
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0018 ===
claim: Raw U+0018 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0018b' 1>"
verbatim input: "<'a\u0018b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0018
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u0019 ===
claim: Raw U+0019 rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u0019b' 1>"
verbatim input: "<'a\u0019b' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u0019
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u001a ===
claim: Raw U+001A rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u001ab' 1>"
verbatim input: "<'a\u001ab' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u001a
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u001b ===
claim: Raw U+001B rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u001bb' 1>"
verbatim input: "<'a\u001bb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u001b
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u001c ===
claim: Raw U+001C rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u001cb' 1>"
verbatim input: "<'a\u001cb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u001c
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u001d ===
claim: Raw U+001D rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u001db' 1>"
verbatim input: "<'a\u001db' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u001d
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u001e ===
claim: Raw U+001E rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u001eb' 1>"
verbatim input: "<'a\u001eb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u001e
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.raw-u001f ===
claim: Raw U+001F rejects in a quoted-name.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "<'a\\u001fb' 1>"
verbatim input: "<'a\u001fb' 1>"
semantic shape: object
slot: quoted-name
variation: —
defect: raw-u001f
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-name.trailing-backslash ===
claim: quoted-name defect trailing-backslash rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'name\\\\\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: trailing-backslash
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 7,
    "index": 6,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 7,
  "index": 6,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-interrupted-apostrophe ===
claim: quoted-name defect unicode-interrupted-apostrophe rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u'000' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-interrupted-apostrophe
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_UNTERMINATED",
  "operation": "tokenize-hson",
  "source": {
    "column": 9,
    "index": 8,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 9,
  "index": 8,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-interrupted-backslash ===
claim: quoted-name defect unicode-interrupted-backslash rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u\\\\000' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-interrupted-backslash
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-interrupted-closer ===
claim: quoted-name defect unicode-interrupted-closer rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u>000' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-interrupted-closer
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-interrupted-quote ===
claim: quoted-name defect unicode-interrupted-quote rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u\\\"000' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-interrupted-quote
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-interrupted-space ===
claim: quoted-name defect unicode-interrupted-space rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u 000' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-interrupted-space
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-one-hex ===
claim: quoted-name defect unicode-one-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u1' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-one-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-three-hex ===
claim: quoted-name defect unicode-three-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u123' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-three-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-two-hex ===
claim: quoted-name defect unicode-two-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u12' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-two-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unicode-zero-hex ===
claim: quoted-name defect unicode-zero-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\u' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unicode-zero-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unsupported-hex ===
claim: quoted-name defect unsupported-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\x41' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unsupported-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unsupported-letter ===
claim: quoted-name defect unsupported-letter rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\q' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unsupported-letter
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unsupported-slash ===
claim: quoted-name defect unsupported-slash rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\/' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unsupported-slash
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-name.unsupported-zero ===
claim: quoted-name defect unsupported-zero rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"<'\\\\0' 1>\""
verbatim input: —
semantic shape: object
slot: quoted-name
variation: —
defect: unsupported-zero
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-name-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-name-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-name, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.eof-during-unicode ===
claim: quoted-string defect eof-during-unicode rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u12\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: eof-during-unicode
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.invalid-hex-position-1 ===
claim: quoted-string defect invalid-hex-position-1 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\uG000\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: invalid-hex-position-1
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.invalid-hex-position-2 ===
claim: quoted-string defect invalid-hex-position-2 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u0G00\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: invalid-hex-position-2
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.invalid-hex-position-3 ===
claim: quoted-string defect invalid-hex-position-3 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u00G0\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: invalid-hex-position-3
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.invalid-hex-position-4 ===
claim: quoted-string defect invalid-hex-position-4 rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u000G\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: invalid-hex-position-4
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.raw-u0000 ===
claim: Raw U+0000 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0000b\""
verbatim input: "\"a\u0000b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0000
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0001 ===
claim: Raw U+0001 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0001b\""
verbatim input: "\"a\u0001b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0001
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0002 ===
claim: Raw U+0002 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0002b\""
verbatim input: "\"a\u0002b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0002
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0003 ===
claim: Raw U+0003 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0003b\""
verbatim input: "\"a\u0003b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0003
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0004 ===
claim: Raw U+0004 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0004b\""
verbatim input: "\"a\u0004b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0004
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0005 ===
claim: Raw U+0005 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0005b\""
verbatim input: "\"a\u0005b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0005
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0006 ===
claim: Raw U+0006 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0006b\""
verbatim input: "\"a\u0006b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0006
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0007 ===
claim: Raw U+0007 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0007b\""
verbatim input: "\"a\u0007b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0007
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0008 ===
claim: Raw U+0008 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0008b\""
verbatim input: "\"a\bb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0008
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0009 ===
claim: Raw U+0009 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0009b\""
verbatim input: "\"a\tb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0009
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u000a ===
claim: Raw U+000A rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u000ab\""
verbatim input: "\"a\nb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u000a
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u000b ===
claim: Raw U+000B rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u000bb\""
verbatim input: "\"a\u000bb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u000b
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u000c ===
claim: Raw U+000C rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u000cb\""
verbatim input: "\"a\fb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u000c
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u000d ===
claim: Raw U+000D rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u000db\""
verbatim input: "\"a\rb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u000d
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u000e ===
claim: Raw U+000E rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u000eb\""
verbatim input: "\"a\u000eb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u000e
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u000f ===
claim: Raw U+000F rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u000fb\""
verbatim input: "\"a\u000fb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u000f
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0010 ===
claim: Raw U+0010 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0010b\""
verbatim input: "\"a\u0010b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0010
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0011 ===
claim: Raw U+0011 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0011b\""
verbatim input: "\"a\u0011b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0011
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0012 ===
claim: Raw U+0012 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0012b\""
verbatim input: "\"a\u0012b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0012
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0013 ===
claim: Raw U+0013 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0013b\""
verbatim input: "\"a\u0013b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0013
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0014 ===
claim: Raw U+0014 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0014b\""
verbatim input: "\"a\u0014b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0014
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0015 ===
claim: Raw U+0015 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0015b\""
verbatim input: "\"a\u0015b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0015
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0016 ===
claim: Raw U+0016 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0016b\""
verbatim input: "\"a\u0016b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0016
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0017 ===
claim: Raw U+0017 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0017b\""
verbatim input: "\"a\u0017b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0017
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0018 ===
claim: Raw U+0018 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0018b\""
verbatim input: "\"a\u0018b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0018
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u0019 ===
claim: Raw U+0019 rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u0019b\""
verbatim input: "\"a\u0019b\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u0019
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u001a ===
claim: Raw U+001A rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u001ab\""
verbatim input: "\"a\u001ab\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u001a
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u001b ===
claim: Raw U+001B rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u001bb\""
verbatim input: "\"a\u001bb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u001b
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u001c ===
claim: Raw U+001C rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u001cb\""
verbatim input: "\"a\u001cb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u001c
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u001d ===
claim: Raw U+001D rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u001db\""
verbatim input: "\"a\u001db\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u001d
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u001e ===
claim: Raw U+001E rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u001eb\""
verbatim input: "\"a\u001eb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u001e
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.raw-u001f ===
claim: Raw U+001F rejects in a quoted-string.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"a\\u001fb\""
verbatim input: "\"a\u001fb\""
semantic shape: scalar
slot: quoted-string
variation: —
defect: raw-u001f
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRING_CONTROL_UNESCAPED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-raw-c0
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, raw-c0, transparent-family, rejection
rationale: The finite 32-case family visibly materializes every raw C0 code point.

=== hson.reject.family.quoted-string.trailing-backslash ===
claim: quoted-string defect trailing-backslash rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"bad\\\\\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: trailing-backslash
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 5,
    "index": 4,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 5,
  "index": 4,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unicode-interrupted-backslash ===
claim: quoted-string defect unicode-interrupted-backslash rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u\\\\000\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unicode-interrupted-backslash
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unicode-interrupted-quote ===
claim: quoted-string defect unicode-interrupted-quote rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u\\\"000\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unicode-interrupted-quote
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unicode-interrupted-space ===
claim: quoted-string defect unicode-interrupted-space rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u 000\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unicode-interrupted-space
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unicode-one-hex ===
claim: quoted-string defect unicode-one-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u1\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unicode-one-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unicode-three-hex ===
claim: quoted-string defect unicode-three-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u123\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unicode-three-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unicode-two-hex ===
claim: quoted-string defect unicode-two-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u12\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unicode-two-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unicode-zero-hex ===
claim: quoted-string defect unicode-zero-hex rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\u\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unicode-zero-hex
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.quoted-string.unsupported-letter ===
claim: quoted-string defect unsupported-letter rejects deterministically.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "\"\\\"\\\\q\\\"\""
verbatim input: —
semantic shape: scalar
slot: quoted-string
variation: —
defect: unsupported-letter
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "invalid-json-escape",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.quoted-string-malformed-escapes
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: quoted-string, escape, transparent-family, rejection
rationale: One lexical slot varies only the malformed escape spelling.

=== hson.reject.family.unsupported-whitespace.u000b ===
claim: Unsupported whitespace U+000B rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u000b"
verbatim input: "1\u000b"
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u000b
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u000c ===
claim: Unsupported whitespace U+000C rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u000c"
verbatim input: "1\f"
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u000c
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u00a0 ===
claim: Unsupported whitespace U+00A0 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u00a0"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u00a0
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u1680 ===
claim: Unsupported whitespace U+1680 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u1680"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u1680
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2000 ===
claim: Unsupported whitespace U+2000 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2000"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2000
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2001 ===
claim: Unsupported whitespace U+2001 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2001"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2001
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2002 ===
claim: Unsupported whitespace U+2002 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2002"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2002
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2003 ===
claim: Unsupported whitespace U+2003 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2003"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2003
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2004 ===
claim: Unsupported whitespace U+2004 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2004"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2004
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2005 ===
claim: Unsupported whitespace U+2005 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2005"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2005
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2006 ===
claim: Unsupported whitespace U+2006 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2006"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2006
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2007 ===
claim: Unsupported whitespace U+2007 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2007"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2007
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2008 ===
claim: Unsupported whitespace U+2008 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2008"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2008
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2009 ===
claim: Unsupported whitespace U+2009 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2009"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2009
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u200a ===
claim: Unsupported whitespace U+200A rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u200a"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u200a
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2028 ===
claim: Unsupported whitespace U+2028 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2028"
verbatim input: "1\u2028"
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2028
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u2029 ===
claim: Unsupported whitespace U+2029 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u2029"
verbatim input: "1\u2029"
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u2029
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u202f ===
claim: Unsupported whitespace U+202F rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u202f"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u202f
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u205f ===
claim: Unsupported whitespace U+205F rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u205f"
verbatim input: "1 "
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u205f
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.family.unsupported-whitespace.u3000 ===
claim: Unsupported whitespace U+3000 rejects outside a quoted token.
classification: materialized-rejected-family-case
ingress format: hson
escaped input: "1\\u3000"
verbatim input: "1　"
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u3000
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_UNSUPPORTED_WHITESPACE",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: family.reject.unsupported-whitespace
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode, transparent-family, rejection
rationale: Only the unsupported whitespace code point varies.

=== hson.reject.literal.array.mismatched-bracket ===
claim: A bracket array must close with a bracket.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"[1,2»\""
verbatim input: —
semantic shape: array
slot: container
variation: —
defect: mismatched-closer
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ARRAY_CLOSER_MISMATCH",
  "operation": "tokenize-hson",
  "source": {
    "column": 5,
    "index": 4,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 5,
  "index": 4,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, grammar
rationale: A bracket array must close with a bracket.

=== hson.reject.literal.array.mismatched-guillemet ===
claim: A guillemet array must close with a guillemet.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"«1,2]\""
verbatim input: —
semantic shape: array
slot: container
variation: —
defect: mismatched-closer
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ARRAY_CLOSER_MISMATCH",
  "operation": "tokenize-hson",
  "source": {
    "column": 5,
    "index": 4,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 5,
  "index": 4,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, grammar
rationale: A guillemet array must close with a guillemet.

=== hson.reject.literal.array.missing-comma ===
claim: Array items require commas.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"[1 2]\""
verbatim input: —
semantic shape: array
slot: array-separator
variation: —
defect: missing-comma
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ARRAY_COMMA_MISSING",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, grammar
rationale: Array items require commas.

=== hson.reject.literal.array.missing-item ===
claim: Two array commas cannot omit an item.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"[1,,2]\""
verbatim input: —
semantic shape: array
slot: array-item
variation: —
defect: missing-item
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ARRAY_ITEM_MISSING",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: array, grammar
rationale: Two array commas cannot omit an item.

=== hson.reject.literal.authored-metadata ===
claim: Authored structural metadata names reject.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e hson:index=\\\"0\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-attribute
variation: —
defect: authored-metadata
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_AUTHORED_METADATA_FORBIDDEN",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: metadata, reserved-name
rationale: Authored structural metadata names reject.

=== hson.reject.literal.comment.block ===
claim: Block comments are unsupported.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"/*x*/1\""
verbatim input: —
semantic shape: source
slot: trivia
variation: —
defect: block-comment
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_BLOCK_COMMENT_UNSUPPORTED",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: comment, grammar
rationale: Block comments are unsupported.

=== hson.reject.literal.element.duplicate-attribute ===
claim: Duplicate decoded element attributes reject with related evidence.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e x=\\\"1\\\" x=\\\"2\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-attribute
variation: —
defect: duplicate-attribute
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_DUPLICATE_ATTRIBUTE",
  "operation": "tokenize-hson",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 4,
        "index": 3,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 10,
    "index": 9,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 10,
  "index": 9,
  "line": 1
}
path evidence: —
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 4,
      "index": 3,
      "line": 1
    }
  }
]
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, duplicate
rationale: Duplicate decoded element attributes reject with related evidence.

=== hson.reject.literal.element.duplicate-quid ===
claim: An element cannot declare two QUIDs.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e @000000001 @000000002/>\""
verbatim input: —
semantic shape: element
slot: element-quid
variation: —
defect: duplicate-quid
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_QUID_INVALID",
  "operation": "tokenize-hson",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 4,
        "index": 3,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 15,
    "index": 14,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 15,
  "index": 14,
  "line": 1
}
path evidence: —
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 4,
      "index": 3,
      "line": 1
    }
  }
]
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, quid
rationale: An element cannot declare two QUIDs.

=== hson.reject.literal.element.flag-after-content ===
claim: Element flags cannot follow content.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e \\\"x\\\" late/>\""
verbatim input: —
semantic shape: element
slot: element-flag
variation: —
defect: after-content
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_HEADER_AFTER_CONTENT",
  "operation": "tokenize-hson",
  "source": {
    "column": 8,
    "index": 7,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 8,
  "index": 7,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, flag
rationale: Element flags cannot follow content.

=== hson.reject.literal.element.malformed-closer ===
claim: Whitespace cannot split an element closer.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e/ >\""
verbatim input: —
semantic shape: element
slot: container
variation: —
defect: malformed-closer
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_CLOSER_MALFORMED",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, grammar
rationale: Whitespace cannot split an element closer.

=== hson.reject.literal.element.missing-attribute-value ===
claim: An explicit element attribute requires a value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e x=/>\""
verbatim input: —
semantic shape: element
slot: element-attribute
variation: —
defect: missing-value
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_ATTRIBUTE_VALUE_INVALID",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, attribute
rationale: An explicit element attribute requires a value.

=== hson.reject.literal.element.missing-quid ===
claim: An element QUID marker requires a persisted QUID.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e @/>\""
verbatim input: —
semantic shape: element
slot: element-quid
variation: —
defect: missing-quid
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_QUID_INVALID",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, quid
rationale: An element QUID marker requires a persisted QUID.

=== hson.reject.literal.element.numeric-content ===
claim: Numeric typed content beneath an HSON element rejects.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e 1/>\""
verbatim input: —
semantic shape: element
slot: element-content
variation: —
defect: typed-content
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_TYPED_CONTENT_FORBIDDEN",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, number, structural-mode
rationale: Numeric typed content beneath an HSON element rejects.

=== hson.reject.literal.empty-attribute-name ===
claim: An empty decoded attribute name rejects.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e ''=\\\"x\\\"/>\""
verbatim input: —
semantic shape: element
slot: element-attribute
variation: —
defect: empty-decoded-name
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_INVALID_START",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, attribute, quoted-name, empty-name
rationale: An empty decoded attribute name rejects.

=== hson.reject.literal.empty-element-name ===
claim: An empty decoded element name rejects.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<''/>\""
verbatim input: —
semantic shape: element
slot: element-name
variation: —
defect: empty-decoded-name
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ELEMENT_NAME_REQUIRED",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, quoted-name, empty-name
rationale: An empty decoded element name rejects.

=== hson.reject.literal.empty-flag-name ===
claim: An empty decoded flag name rejects.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e ''/>\""
verbatim input: —
semantic shape: element
slot: element-flag
variation: —
defect: empty-decoded-name
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_INVALID_START",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: element, flag, quoted-name, empty-name
rationale: An empty decoded flag name rejects.

=== hson.reject.literal.legacy-backtick-name ===
claim: Legacy backtick-delimited names are invalid.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<`legacy name` 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: —
defect: legacy-backtick-name
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_LEGACY_BACKTICK",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, quoted-name, legacy, rejection
rationale: Legacy backtick-delimited names are invalid.

=== hson.reject.literal.mode.array-element ===
claim: An array cannot contain element-mode content.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"[<e/>]\""
verbatim input: —
semantic shape: array
slot: array-item
variation: —
defect: element-in-array
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRUCTURAL_MODE_CROSSING",
  "operation": "parse_tokens.structural-mode",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "structural-mode-admission"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: structural-mode
rationale: An array cannot contain element-mode content.

=== hson.reject.literal.mode.element-array ===
claim: An HSON element cannot contain an array.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e [1]/>\""
verbatim input: —
semantic shape: element
slot: element-content
variation: —
defect: array-beneath-element
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRUCTURAL_MODE_CROSSING",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: structural-mode
rationale: An HSON element cannot contain an array.

=== hson.reject.literal.mode.element-object ===
claim: An HSON element cannot contain object structure.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<e <b 1>/>\""
verbatim input: —
semantic shape: element
slot: element-content
variation: —
defect: object-beneath-element
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_STRUCTURAL_MODE_CROSSING",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: structural-mode
rationale: An HSON element cannot contain object structure.

=== hson.reject.literal.object.attribute-syntax ===
claim: Object properties do not use attribute equals syntax.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a title=\\\"x\\\" \\\"v\\\">\""
verbatim input: —
semantic shape: object
slot: property-key
variation: —
defect: attribute-syntax
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_OBJECT_ATTRIBUTE_FORBIDDEN",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, grammar
rationale: Object properties do not use attribute equals syntax.

=== hson.reject.literal.object.comma ===
claim: Object properties do not use commas.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a 1, b 2>\""
verbatim input: —
semantic shape: object
slot: property-separator
variation: —
defect: comma
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_OBJECT_COMMA_FORBIDDEN",
  "operation": "tokenize-hson",
  "source": {
    "column": 5,
    "index": 4,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 5,
  "index": 4,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, grammar
rationale: Object properties do not use commas.

=== hson.reject.literal.object.duplicate ===
claim: Duplicate decoded object-property keys reject with related evidence.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a 1 a 2>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: —
defect: duplicate-key
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_OBJECT_DUPLICATE_MEMBER",
  "operation": "tokenize-hson",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 2,
        "index": 1,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 6,
    "index": 5,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 6,
  "index": 5,
  "line": 1
}
path evidence: —
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 2,
      "index": 1,
      "line": 1
    }
  }
]
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, duplicate
rationale: Duplicate decoded object-property keys reject with related evidence.

=== hson.reject.literal.object.extra-value ===
claim: An object property has exactly one value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a 1 2 3>\""
verbatim input: —
semantic shape: object
slot: property-value
variation: —
defect: extra-value
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_OBJECT_EXTRA_VALUE",
  "operation": "tokenize-hson",
  "source": {
    "column": 6,
    "index": 5,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 6,
  "index": 5,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, grammar
rationale: An object property has exactly one value.

=== hson.reject.literal.object.flag ===
claim: An object property cannot omit its value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a flag>\""
verbatim input: —
semantic shape: object
slot: property-value
variation: —
defect: flag
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_OBJECT_FLAG_FORBIDDEN",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, grammar
rationale: An object property cannot omit its value.

=== hson.reject.literal.object.legacy-adjacent ===
claim: Adjacent angle objects do not merge into one object.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a 1><b 2>\""
verbatim input: —
semantic shape: object
slot: root
variation: —
defect: legacy-syntax
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_LEGACY_ADJACENT_OBJECT",
  "operation": "parse_tokens.root-shaping",
  "source": {
    "column": 6,
    "index": 5,
    "line": 1
  },
  "stage": "root-shaping"
}
source evidence:
{
  "column": 6,
  "index": 5,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, legacy
rationale: Adjacent angle objects do not merge into one object.

=== hson.reject.literal.object.legacy-doubled ===
claim: Legacy doubled-angle object syntax rejects.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<<a 1>>\""
verbatim input: —
semantic shape: object
slot: container
variation: —
defect: legacy-syntax
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "legacy-doubled-object-syntax",
  "operation": "tokenize-hson",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, legacy
rationale: Legacy doubled-angle object syntax rejects.

=== hson.reject.literal.object.missing-trivia ===
claim: A property key and value require trivia.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a\\\"x\\\">\""
verbatim input: —
semantic shape: object
slot: property-separator
variation: —
defect: missing-trivia
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_REQUIRED_TRIVIA_MISSING",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, grammar
rationale: A property key and value require trivia.

=== hson.reject.literal.object.quid ===
claim: Object-property QUIDs do not exist in authored HSON.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a @000000001 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: —
defect: object-quid
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_OBJECT_QUID_FORBIDDEN",
  "operation": "tokenize-hson",
  "source": {
    "column": 4,
    "index": 3,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 4,
  "index": 3,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, quid
rationale: Object-property QUIDs do not exist in authored HSON.

=== hson.reject.literal.quoted-name.raw-apostrophe ===
claim: A literal apostrophe inside a quoted name must be escaped.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<'don't' 1>\""
verbatim input: —
semantic shape: object
slot: property-key
variation: —
defect: raw-apostrophe
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NAME_UNTERMINATED",
  "operation": "tokenize-hson",
  "source": {
    "column": 8,
    "index": 7,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 8,
  "index": 7,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, quoted-name, apostrophe, rejection
rationale: A literal apostrophe inside a quoted name must be escaped.

=== hson.reject.literal.reserved-name ===
claim: Authored _hson_* element names reject.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<_hson_obj/>\""
verbatim input: —
semantic shape: element
slot: element-name
variation: —
defect: reserved-name
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "authored-reserved-name",
  "operation": "tokenize-hson.authored-name",
  "source": {
    "column": 2,
    "index": 1,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 2,
  "index": 1,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: reserved-name
rationale: Authored _hson_* element names reject.

=== hson.reject.literal.root.bare-name ===
claim: An arbitrary bare name is not a root primitive.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"value\""
verbatim input: —
semantic shape: source
slot: root-value
variation: —
defect: bare-name
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_PRIMITIVE_TOKEN_INVALID",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: grammar
rationale: An arbitrary bare name is not a root primitive.

=== hson.reject.literal.root.mixed-modes ===
claim: Element and object root modes cannot mix.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<a/><b 2>\""
verbatim input: —
semantic shape: mixed-root
slot: root
variation: —
defect: mixed-mode
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ROOT_MIXED_MODES",
  "operation": "parse_tokens.structural-mode",
  "source": {
    "column": 5,
    "index": 4,
    "line": 1
  },
  "stage": "root-shaping"
}
source evidence:
{
  "column": 5,
  "index": 4,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: structural-mode
rationale: Element and object root modes cannot mix.

=== hson.reject.literal.root.multiple-values ===
claim: A root contains exactly one semantic value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"1 2\""
verbatim input: —
semantic shape: source
slot: root
variation: —
defect: multiple-values
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_ROOT_MULTIPLE_VALUES",
  "operation": "parse_tokens.root-shaping",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "root-shaping"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: root-boundary
rationale: A root contains exactly one semantic value.

=== hson.reject.literal.root.trailing-closer ===
claim: Trailing source after a primitive rejects.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"42>\""
verbatim input: —
semantic shape: source
slot: root
variation: —
defect: trailing-source
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_TRAILING_SOURCE",
  "operation": "tokenize-hson",
  "source": {
    "column": 3,
    "index": 2,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 3,
  "index": 2,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: root-boundary
rationale: Trailing source after a primitive rejects.

=== hson.reject.literal.single-quoted-value ===
claim: Single quotes do not delimit object string values.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"<'name' 'value'>\""
verbatim input: —
semantic shape: object
slot: property-value
variation: —
defect: single-quoted-value
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_QUOTE_KIND_UNSUPPORTED",
  "operation": "tokenize-hson",
  "source": {
    "column": 9,
    "index": 8,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 9,
  "index": 8,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: object, string, quote-kind, rejection
rationale: Single quotes do not delimit object string values.

=== hson.reject.literal.source.comment-only ===
claim: Comment-only source has no semantic value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"// comment\""
verbatim input: —
semantic shape: source
slot: root
variation: —
defect: empty-source
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_SOURCE_EMPTY",
  "operation": "parse_hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "source-admission"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: source-admission, comment
rationale: Comment-only source has no semantic value.

=== hson.reject.literal.source.empty ===
claim: Empty source has no semantic value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"\""
verbatim input: —
semantic shape: source
slot: root
variation: —
defect: empty-source
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_SOURCE_EMPTY",
  "operation": "parse_hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "source-admission"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: source-admission
rationale: Empty source has no semantic value.

=== hson.reject.literal.source.whitespace-only ===
claim: Whitespace-only source has no semantic value.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"   \""
verbatim input: —
semantic shape: source
slot: root
variation: —
defect: empty-source
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_SOURCE_EMPTY",
  "operation": "parse_hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "source-admission"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: source-admission, trivia
rationale: Whitespace-only source has no semantic value.

=== hson.reject.literal.whitespace.byte-order-mark ===
claim: U+FEFF is not authored-HSON trivia.
classification: literal-rejected-authored-hson
ingress format: hson
escaped input: "\"1﻿\""
verbatim input: —
semantic shape: scalar
slot: trivia
variation: —
defect: unsupported-u-feff
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_NUMBER_TRAILING_JUNK",
  "operation": "tokenize-hson",
  "source": {
    "column": 1,
    "index": 0,
    "line": 1
  },
  "stage": "tokenization"
}
source evidence:
{
  "column": 1,
  "index": 0,
  "line": 1
}
path evidence: —
related evidence: —
origin: settled-authored-hson-language
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: trivia, unicode
rationale: U+FEFF is not authored-HSON trivia.

=== html.accept.control-empty-unicode-adjacent ===
claim: Ordinary, empty, control-bearing, and Unicode leaves remain separately ordered.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ordinary\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\u0000\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"😀\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\r\\n\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: control-empty-unicode-adjacent
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "ordinary"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "\u0000"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "😀"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "\r\n"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<div><_hson_elem><_hson_str>&quot;ordinary&quot;</_hson_str><_hson_str>&quot;&quot;</_hson_str><_hson_str>&quot;\\u0000&quot;</_hson_str><_hson_str>&quot;\\ud83d\\ude00&quot;</_hson_str><_hson_str>&quot;\\r\\n&quot;</_hson_str></_hson_elem></div>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "ordinary",
  "",
  "\u0000",
  "😀",
  "\r\n"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Ordinary, empty, control-bearing, and Unicode leaves remain separately ordered.

=== html.accept.empty-nonempty-empty ===
claim: Empty/nonempty/empty string order survives.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"x\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: empty-nonempty-empty
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "x"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<div><_hson_elem><_hson_str>&quot;&quot;</_hson_str><_hson_str>&quot;x&quot;</_hson_str><_hson_str>&quot;&quot;</_hson_str></_hson_elem></div>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "",
  "x",
  ""
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Empty/nonempty/empty string order survives.

=== html.accept.identical-adjacent ===
claim: Identical adjacent strings do not merge.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"same\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"same\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: identical-adjacent
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "same"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "same"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<div><_hson_elem><_hson_str>&quot;same&quot;</_hson_str><_hson_str>&quot;same&quot;</_hson_str></_hson_elem></div>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "same",
  "same"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Identical adjacent strings do not merge.

=== html.accept.ingress.element-string-empty ===
claim: An HTML HSON element carrier detaches empty text.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_str>&quot;&quot;</_hson_str></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: element-string-empty
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  ""
]
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON element carrier detaches empty text.

=== html.accept.ingress.element-string-ordinary ===
claim: An HTML HSON element carrier detaches ordinary text.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_str>&quot;ordinary&quot;</_hson_str></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: element-string-ordinary
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "ordinary"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;ordinary&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "ordinary"
]
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON element carrier detaches ordinary text.

=== html.accept.ingress.object-false ===
claim: An HTML HSON object carrier detaches typed false.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_val>false</_hson_val></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-false
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    false
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_val>false</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches typed false.

=== html.accept.ingress.object-negative-number ===
claim: An HTML HSON object carrier detaches a negative finite number.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_val>-12.5</_hson_val></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-negative-number
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    -12.5
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_val>-12.5</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches a negative finite number.

=== html.accept.ingress.object-negative-zero ===
claim: An HTML HSON object carrier detaches negative zero.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_val>-0</_hson_val></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-negative-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$number": "-0"
    }
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_val>-0</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths:
[
  "$.$_content[0]"
]
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches negative zero.

=== html.accept.ingress.object-null ===
claim: An HTML HSON object carrier detaches typed null.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_val>null</_hson_val></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-null
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    null
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_val>null</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches typed null.

=== html.accept.ingress.object-positive-number ===
claim: An HTML HSON object carrier detaches a positive finite number.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_val>12.5</_hson_val></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-positive-number
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    12.5
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_val>12.5</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches a positive finite number.

=== html.accept.ingress.object-string-empty ===
claim: An HTML HSON object carrier detaches an empty string.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_str>&quot;&quot;</_hson_str></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-string-empty
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  ""
]
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches an empty string.

=== html.accept.ingress.object-string-ordinary ===
claim: An HTML HSON object carrier detaches an ordinary string.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_str>&quot;ordinary&quot;</_hson_str></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-string-ordinary
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "ordinary"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;ordinary&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "ordinary"
]
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches an ordinary string.

=== html.accept.ingress.object-true ===
claim: An HTML HSON object carrier detaches typed true.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_val>true</_hson_val></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-true
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    true
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_val>true</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches typed true.

=== html.accept.ingress.object-zero ===
claim: An HTML HSON object carrier detaches zero.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_obj><_hson_val>0</_hson_val></_hson_obj>\""
verbatim input: —
semantic shape: structural-html
slot: detached-semantic-boundary
variation: object-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    0
  ],
  "$_tag": "_hson_val"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_val>0</_hson_val></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, scalar-carrier
rationale: An HTML HSON object carrier detaches zero.

=== html.accept.ordinary-melted-text ===
claim: One unambiguous ordinary HTML text leaf may remain melted.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ordinary\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: ordinary-melted-text
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "ordinary"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<div>ordinary</div>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "ordinary"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, ordinary-html
rationale: One unambiguous ordinary HTML text leaf may remain melted.

=== html.accept.script-line-ending-carrier ===
claim: Script text with LF uses an explicit structural carrier to preserve code units.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"script\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"const x={\\nvalue:\\\"<>&/\\\"\\n};\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: script-line-ending-carrier
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "const x={\nvalue:\"<>&/\"\n};"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "script"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<script><_hson_elem><_hson_str>&quot;const x={\\nvalue:\\&quot;&lt;&gt;&amp;/\\&quot;\\n};&quot;</_hson_str></_hson_elem></script>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "const x={\nvalue:\"<>&/\"\n};"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Script text with LF uses an explicit structural carrier to preserve code units.

=== html.accept.script-ordinary-raw-text ===
claim: Ordinary script raw text preserves braces, quotes, angle characters, ampersands, slashes, comments, JSON-looking text, and HSON-looking text.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"script\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"const x={value:\\\"<>&/\\\",note:\\\"// /* */ <a/> «1,2»\\\"};\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: script-ordinary-raw-text
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "const x={value:\"<>&/\",note:\"// /* */ <a/> «1,2»\"};"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "script"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<script>const x={value:\"<>&/\",note:\"// /* */ <a/> «1,2»\"};</script>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "const x={value:\"<>&/\",note:\"// /* */ <a/> «1,2»\"};"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, ordinary-html
rationale: Ordinary script raw text preserves braces, quotes, angle characters, ampersands, slashes, comments, JSON-looking text, and HSON-looking text.

=== html.accept.script-segmented-carrier ===
claim: Explicit structural carriers override script raw-text melting when segmentation matters.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"script\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"a\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"b\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: script-segmented-carrier
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "b"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "script"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<script><_hson_elem><_hson_str>&quot;a&quot;</_hson_str><_hson_str>&quot;&quot;</_hson_str><_hson_str>&quot;b&quot;</_hson_str></_hson_elem></script>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "a",
  "",
  "b"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit structural carriers override script raw-text melting when segmentation matters.

=== html.accept.string-ampersand ===
claim: Explicit string transport preserves ampersand exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"&\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-ampersand
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "&"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;&amp;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "&"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves ampersand exactly.

=== html.accept.string-backslash ===
claim: Explicit string transport preserves backslash exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\\\\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-backslash
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\\"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\\\&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\\"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves backslash exactly.

=== html.accept.string-backspace ===
claim: Explicit string transport preserves backspace exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\b\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-backspace
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\b"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\b&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\b"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves backspace exactly.

=== html.accept.string-bmp-non-ascii ===
claim: Explicit string transport preserves bmp-non-ascii exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"λ漢\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-bmp-non-ascii
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "λ漢"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\u03bb\\u6f22&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "λ漢"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves bmp-non-ascii exactly.

=== html.accept.string-carriage-return ===
claim: Explicit string transport preserves carriage-return exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\r\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-carriage-return
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\r"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\r&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\r"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves carriage-return exactly.

=== html.accept.string-crlf ===
claim: Explicit string transport preserves crlf exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\r\\n\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-crlf
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\r\n"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\r\\n&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\r\n"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves crlf exactly.

=== html.accept.string-form-feed ===
claim: Explicit string transport preserves form-feed exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\f\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-form-feed
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\f"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\f&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\f"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves form-feed exactly.

=== html.accept.string-greater-than ===
claim: Explicit string transport preserves greater-than exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\">\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-greater-than
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    ">"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;&gt;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  ">"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves greater-than exactly.

=== html.accept.string-isolated-high-surrogate ===
claim: Explicit string transport preserves isolated-high-surrogate exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\ud800\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-isolated-high-surrogate
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\ud800"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\ud800&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\ud800"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves isolated-high-surrogate exactly.

=== html.accept.string-isolated-low-surrogate ===
claim: Explicit string transport preserves isolated-low-surrogate exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\udc00\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-isolated-low-surrogate
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\udc00"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\udc00&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\udc00"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves isolated-low-surrogate exactly.

=== html.accept.string-less-than ===
claim: Explicit string transport preserves less-than exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"<\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-less-than
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "<"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;&lt;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "<"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves less-than exactly.

=== html.accept.string-line-feed ===
claim: Explicit string transport preserves line-feed exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\n\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-line-feed
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\n"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\n&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\n"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves line-feed exactly.

=== html.accept.string-nul ===
claim: Explicit string transport preserves nul exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\u0000\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-nul
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\u0000"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\u0000&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\u0000"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves nul exactly.

=== html.accept.string-quote ===
claim: Explicit string transport preserves quote exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\\"\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-quote
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\""
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\&quot;&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\""
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves quote exactly.

=== html.accept.string-slash ===
claim: Explicit string transport preserves slash exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"/\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-slash
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "/"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;/&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "/"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves slash exactly.

=== html.accept.string-supplementary-unicode ===
claim: Explicit string transport preserves supplementary-unicode exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"😀\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-supplementary-unicode
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "😀"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\ud83d\\ude00&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "😀"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves supplementary-unicode exactly.

=== html.accept.string-tab ===
claim: Explicit string transport preserves tab exactly.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_str\",\"$_content\":[\"\\t\"]}"
verbatim input: —
semantic shape: _hson_str
slot: html-transport
variation: string-tab
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    "\t"
  ],
  "$_tag": "_hson_str"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<_hson_obj><_hson_str>&quot;\\t&quot;</_hson_str></_hson_obj>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "\t"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit string transport preserves tab exactly.

=== html.accept.style-line-ending-carrier ===
claim: Style text with CRLF uses an explicit structural carrier to preserve code units.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"style\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"a{\\r\\nb:c\\r\\n}\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: style-line-ending-carrier
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a{\r\nb:c\r\n}"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "style"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<style><_hson_elem><_hson_str>&quot;a{\\r\\nb:c\\r\\n}&quot;</_hson_str></_hson_elem></style>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "a{\r\nb:c\r\n}"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Style text with CRLF uses an explicit structural carrier to preserve code units.

=== html.accept.style-ordinary-raw-text ===
claim: Ordinary style raw text preserves braces, quotes, angle characters, ampersands, slashes, comments, JSON-looking text, and HSON-looking text.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"style\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"a{content:\\\"<>&/\\\"}/* // */{\\\"x\\\":1}<a/>«1,2»\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: style-ordinary-raw-text
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a{content:\"<>&/\"}/* // */{\"x\":1}<a/>«1,2»"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "style"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<style>a{content:\"<>&/\"}/* // */{\"x\":1}<a/>«1,2»</style>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "a{content:\"<>&/\"}/* // */{\"x\":1}<a/>«1,2»"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, ordinary-html
rationale: Ordinary style raw text preserves braces, quotes, angle characters, ampersands, slashes, comments, JSON-looking text, and HSON-looking text.

=== html.accept.style-segmented-carrier ===
claim: Explicit structural carriers override style raw-text melting when segmentation matters.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"style\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"a\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"b\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: style-segmented-carrier
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "b"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "style"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<style><_hson_elem><_hson_str>&quot;a&quot;</_hson_str><_hson_str>&quot;&quot;</_hson_str><_hson_str>&quot;b&quot;</_hson_str></_hson_elem></style>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "a",
  "",
  "b"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Explicit structural carriers override style raw-text melting when segmentation matters.

=== html.accept.three-adjacent-strings ===
claim: Three adjacent strings remain three leaves.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"a\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"b\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"c\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: three-adjacent-strings
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "b"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "c"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<div><_hson_elem><_hson_str>&quot;a&quot;</_hson_str><_hson_str>&quot;b&quot;</_hson_str><_hson_str>&quot;c&quot;</_hson_str></_hson_elem></div>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "a",
  "b",
  "c"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Three adjacent strings remain three leaves.

=== html.accept.three-empty-strings ===
claim: Multiple empty strings retain their exact count.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: three-empty-strings
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                ""
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<div><_hson_elem><_hson_str>&quot;&quot;</_hson_str><_hson_str>&quot;&quot;</_hson_str><_hson_str>&quot;&quot;</_hson_str></_hson_elem></div>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "",
  "",
  ""
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Multiple empty strings retain their exact count.

=== html.accept.two-adjacent-strings ===
claim: Two adjacent strings remain two leaves.
classification: structural-html-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"div\",\"$_content\":[{\"$_tag\":\"_hson_elem\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"a\"]},{\"$_tag\":\"_hson_str\",\"$_content\":[\"b\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_elem
slot: html-transport
variation: two-adjacent-strings
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a"
              ],
              "$_tag": "_hson_str"
            },
            {
              "$_content": [
                "b"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_elem"
        }
      ],
      "$_tag": "div"
    }
  ],
  "$_tag": "_hson_elem"
}
exact HSON output: —
exact JSON output: —
exact HTML output: "<div><_hson_elem><_hson_str>&quot;a&quot;</_hson_str><_hson_str>&quot;b&quot;</_hson_str></_hson_elem></div>"
exact diagnostic output: —
expected key sequences: —
expected string leaves:
[
  "a",
  "b"
]
negative-zero paths: —
structured rejection: —
origin: transport-totality
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, transport, structural-transport
rationale: Two adjacent strings remain two leaves.

=== html.reject.direct-element-false ===
claim: Structural HTML forbids typed false beneath an HSON element carrier.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_val>false</_hson_val></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: direct-element-false
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "TRANSFORM_ERROR",
  "operation": "parse-html-string"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML forbids typed false beneath an HSON element carrier.

=== html.reject.direct-element-negative-number ===
claim: Structural HTML forbids a typed negative number beneath an HSON element carrier.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_val>-12.5</_hson_val></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: direct-element-negative-number
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "TRANSFORM_ERROR",
  "operation": "parse-html-string"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML forbids a typed negative number beneath an HSON element carrier.

=== html.reject.direct-element-negative-zero ===
claim: Structural HTML forbids typed negative zero beneath an HSON element carrier.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_val>-0</_hson_val></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: direct-element-negative-zero
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "TRANSFORM_ERROR",
  "operation": "parse-html-string"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML forbids typed negative zero beneath an HSON element carrier.

=== html.reject.direct-element-null ===
claim: Structural HTML forbids typed null beneath an HSON element carrier.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_val>null</_hson_val></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: direct-element-null
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "TRANSFORM_ERROR",
  "operation": "parse-html-string"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML forbids typed null beneath an HSON element carrier.

=== html.reject.direct-element-true ===
claim: Structural HTML forbids typed true beneath an HSON element carrier.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_val>true</_hson_val></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: direct-element-true
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "TRANSFORM_ERROR",
  "operation": "parse-html-string"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML forbids typed true beneath an HSON element carrier.

=== html.reject.direct-element-value ===
claim: Structural HTML forbids a direct typed value beneath an HSON element carrier.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_val>1</_hson_val></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: direct-element-value
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "TRANSFORM_ERROR",
  "operation": "parse-html-string"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML forbids a direct typed value beneath an HSON element carrier.

=== html.reject.direct-element-zero ===
claim: Structural HTML forbids typed zero beneath an HSON element carrier.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><_hson_val>0</_hson_val></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: direct-element-zero
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "TRANSFORM_ERROR",
  "operation": "parse-html-string"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML forbids typed zero beneath an HSON element carrier.

=== html.reject.nested-element-value ===
claim: Structural HTML rejects a typed value beneath an ordinary element in element mode.
classification: structural-html-transport
ingress format: html
escaped input: "\"<_hson_elem><div><_hson_val>true</_hson_val></div></_hson_elem>\""
verbatim input: —
semantic shape: structural-html
slot: element-content
variation: —
defect: nested-element-value
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_CANONICAL_INVARIANT_VIOLATION",
  "operation": "parse-html-string",
  "stage": "canonical-invariant-admission"
}
source evidence: —
path evidence: —
related evidence: —
origin: mode-sensitive-scalar-normalization
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: html, typed-value, structural-mode, rejection
rationale: Structural HTML rejects a typed value beneath an ordinary element in element mode.

=== json.accept.structural-json-order.array-inside-object ===
claim: Structural JSON preserves the handwritten canonical property sequence for structural-json-order.array-inside-object.
classification: structural-json-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"before\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"first\"]}]}]},{\"$_tag\":\"items\",\"$_content\":[{\"$_tag\":\"_hson_arr\",\"$_content\":[{\"$_tag\":\"_hson_ii\",\"$_meta\":{\"index\":\"0\"},\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"10\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ten\"]}]}]},{\"$_tag\":\"2\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"two\"]}]}]},{\"$_tag\":\"1\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"one\"]}]}]}]}]},{\"$_tag\":\"_hson_ii\",\"$_meta\":{\"index\":\"1\"},\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"3\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"three\"]}]}]},{\"$_tag\":\"0\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"zero\"]}]}]}]}]}]}]},{\"$_tag\":\"after\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"last\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: object-property-order
variation: structural-json-order.array-inside-object
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "first"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "before"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        {
                          "$_content": [
                            {
                              "$_content": [
                                "ten"
                              ],
                              "$_tag": "_hson_str"
                            }
                          ],
                          "$_tag": "_hson_obj"
                        }
                      ],
                      "$_tag": "10"
                    },
                    {
                      "$_content": [
                        {
                          "$_content": [
                            {
                              "$_content": [
                                "two"
                              ],
                              "$_tag": "_hson_str"
                            }
                          ],
                          "$_tag": "_hson_obj"
                        }
                      ],
                      "$_tag": "2"
                    },
                    {
                      "$_content": [
                        {
                          "$_content": [
                            {
                              "$_content": [
                                "one"
                              ],
                              "$_tag": "_hson_str"
                            }
                          ],
                          "$_tag": "_hson_obj"
                        }
                      ],
                      "$_tag": "1"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_meta": {
                "index": "0"
              },
              "$_tag": "_hson_ii"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        {
                          "$_content": [
                            {
                              "$_content": [
                                "three"
                              ],
                              "$_tag": "_hson_str"
                            }
                          ],
                          "$_tag": "_hson_obj"
                        }
                      ],
                      "$_tag": "3"
                    },
                    {
                      "$_content": [
                        {
                          "$_content": [
                            {
                              "$_content": [
                                "zero"
                              ],
                              "$_tag": "_hson_str"
                            }
                          ],
                          "$_tag": "_hson_obj"
                        }
                      ],
                      "$_tag": "0"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_meta": {
                "index": "1"
              },
              "$_tag": "_hson_ii"
            }
          ],
          "$_tag": "_hson_arr"
        }
      ],
      "$_tag": "items"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "last"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "after"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: "{\n  \"before\": \"first\",\n  \"items\": [\n    {\n      \"10\": \"ten\",\n      \"2\": \"two\",\n      \"1\": \"one\"\n    },\n    {\n      \"3\": \"three\",\n      \"0\": \"zero\"\n    }\n  ],\n  \"after\": \"last\"\n}"
exact HTML output: —
exact diagnostic output: —
expected key sequences:
[
  [
    "before",
    "items",
    "after"
  ],
  [
    "10",
    "2",
    "1"
  ],
  [
    "3",
    "0"
  ]
]
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: structural-json-order.array-inside-object
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/fixtures/structural-json-order-fixtures.mts#structural-json-order.array-inside-object"
]
declared source reuse: —
tags: json, property-order, transport
rationale: The graph, JSON text, and expected nested key sequences are handwritten in the ordered JSON fixture inventory.

=== json.accept.structural-json-order.direct-integer-keys ===
claim: Structural JSON preserves the handwritten canonical property sequence for structural-json-order.direct-integer-keys.
classification: structural-json-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"10\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ten\"]}]}]},{\"$_tag\":\"2\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"two\"]}]}]},{\"$_tag\":\"1\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"one\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: object-property-order
variation: structural-json-order.direct-integer-keys
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "ten"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "10"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "two"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "2"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "one"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "1"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: "{\n  \"10\": \"ten\",\n  \"2\": \"two\",\n  \"1\": \"one\"\n}"
exact HTML output: —
exact diagnostic output: —
expected key sequences:
[
  [
    "10",
    "2",
    "1"
  ]
]
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: structural-json-order.direct-integer-keys
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/fixtures/structural-json-order-fixtures.mts#structural-json-order.direct-integer-keys"
]
declared source reuse: —
tags: json, property-order, transport
rationale: The graph, JSON text, and expected nested key sequences are handwritten in the ordered JSON fixture inventory.

=== json.accept.structural-json-order.mixed-key-classes ===
claim: Structural JSON preserves the handwritten canonical property sequence for structural-json-order.mixed-key-classes.
classification: structural-json-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"a\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"a\"]}]}]},{\"$_tag\":\"10\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ten\"]}]}]},{\"$_tag\":\"2\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"two\"]}]}]},{\"$_tag\":\"01\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"leading-zero\"]}]}]},{\"$_tag\":\"4294967294\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"highest-index\"]}]}]},{\"$_tag\":\"4294967295\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"outside-index\"]}]}]},{\"$_tag\":\"-1\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"negative\"]}]}]},{\"$_tag\":\"b\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"b\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: object-property-order
variation: structural-json-order.mixed-key-classes
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "a"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "a"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "ten"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "10"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "two"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "2"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "leading-zero"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "01"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "highest-index"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "4294967294"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "outside-index"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "4294967295"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "negative"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "-1"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "b"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "b"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: "{\n  \"a\": \"a\",\n  \"10\": \"ten\",\n  \"2\": \"two\",\n  \"01\": \"leading-zero\",\n  \"4294967294\": \"highest-index\",\n  \"4294967295\": \"outside-index\",\n  \"-1\": \"negative\",\n  \"b\": \"b\"\n}"
exact HTML output: —
exact diagnostic output: —
expected key sequences:
[
  [
    "a",
    "10",
    "2",
    "01",
    "4294967294",
    "4294967295",
    "-1",
    "b"
  ]
]
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: structural-json-order.mixed-key-classes
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/fixtures/structural-json-order-fixtures.mts#structural-json-order.mixed-key-classes"
]
declared source reuse: —
tags: json, property-order, transport
rationale: The graph, JSON text, and expected nested key sequences are handwritten in the ordered JSON fixture inventory.

=== json.accept.structural-json-order.negative-zero ===
claim: Structural JSON preserves the handwritten canonical property sequence for structural-json-order.negative-zero.
classification: structural-json-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"10\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ten\"]}]}]},{\"$_tag\":\"negative-zero\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_val\",\"$_content\":[0]}]}]},{\"$_tag\":\"2\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"two\"]}]}]},{\"$_tag\":\"1\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"one\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: object-property-order
variation: structural-json-order.negative-zero
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "ten"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "10"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$number": "-0"
                }
              ],
              "$_tag": "_hson_val"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "negative-zero"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "two"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "2"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "one"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "1"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: "{\n  \"10\": \"ten\",\n  \"negative-zero\": -0,\n  \"2\": \"two\",\n  \"1\": \"one\"\n}"
exact HTML output: —
exact diagnostic output: —
expected key sequences:
[
  [
    "10",
    "negative-zero",
    "2",
    "1"
  ]
]
expected string leaves: —
negative-zero paths:
[
  "$.$_content[1].$_content[0].$_content[0].$_content[0]"
]
structured rejection: —
origin: structural-json-order.negative-zero
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/fixtures/structural-json-order-fixtures.mts#structural-json-order.negative-zero"
]
declared source reuse: —
tags: json, property-order, transport
rationale: The graph, JSON text, and expected nested key sequences are handwritten in the ordered JSON fixture inventory.

=== json.accept.structural-json-order.nested-object ===
claim: Structural JSON preserves the handwritten canonical property sequence for structural-json-order.nested-object.
classification: structural-json-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"outer-z\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"first\"]}]}]},{\"$_tag\":\"inner\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"10\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ten\"]}]}]},{\"$_tag\":\"2\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"two\"]}]}]},{\"$_tag\":\"1\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"one\"]}]}]}]}]},{\"$_tag\":\"outer-a\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"last\"]}]}]}]}"
verbatim input: —
semantic shape: _hson_obj
slot: object-property-order
variation: structural-json-order.nested-object
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "first"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "outer-z"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "ten"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "10"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "two"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "2"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "one"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "1"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "inner"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "last"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "outer-a"
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: "{\n  \"outer-z\": \"first\",\n  \"inner\": {\n    \"10\": \"ten\",\n    \"2\": \"two\",\n    \"1\": \"one\"\n  },\n  \"outer-a\": \"last\"\n}"
exact HTML output: —
exact diagnostic output: —
expected key sequences:
[
  [
    "outer-z",
    "inner",
    "outer-a"
  ],
  [
    "10",
    "2",
    "1"
  ]
]
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: structural-json-order.nested-object
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/fixtures/structural-json-order-fixtures.mts#structural-json-order.nested-object"
]
declared source reuse: —
tags: json, property-order, transport
rationale: The graph, JSON text, and expected nested key sequences are handwritten in the ordered JSON fixture inventory.

=== json.accept.structural-json-order.object-inside-array ===
claim: Structural JSON preserves the handwritten canonical property sequence for structural-json-order.object-inside-array.
classification: structural-json-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_arr\",\"$_content\":[{\"$_tag\":\"_hson_ii\",\"$_meta\":{\"index\":\"0\"},\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"10\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"ten\"]}]}]},{\"$_tag\":\"2\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"two\"]}]}]},{\"$_tag\":\"1\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"one\"]}]}]}]}]},{\"$_tag\":\"_hson_ii\",\"$_meta\":{\"index\":\"1\"},\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"tail\"]}]}]}"
verbatim input: —
semantic shape: _hson_arr
slot: object-property-order
variation: structural-json-order.object-inside-array
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "ten"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "10"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "two"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "2"
            },
            {
              "$_content": [
                {
                  "$_content": [
                    {
                      "$_content": [
                        "one"
                      ],
                      "$_tag": "_hson_str"
                    }
                  ],
                  "$_tag": "_hson_obj"
                }
              ],
              "$_tag": "1"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_meta": {
        "index": "0"
      },
      "$_tag": "_hson_ii"
    },
    {
      "$_content": [
        {
          "$_content": [
            "tail"
          ],
          "$_tag": "_hson_str"
        }
      ],
      "$_meta": {
        "index": "1"
      },
      "$_tag": "_hson_ii"
    }
  ],
  "$_tag": "_hson_arr"
}
exact HSON output: —
exact JSON output: "[\n  {\n    \"10\": \"ten\",\n    \"2\": \"two\",\n    \"1\": \"one\"\n  },\n  \"tail\"\n]"
exact HTML output: —
exact diagnostic output: —
expected key sequences:
[
  [
    "10",
    "2",
    "1"
  ]
]
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: structural-json-order.object-inside-array
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/fixtures/structural-json-order-fixtures.mts#structural-json-order.object-inside-array"
]
declared source reuse: —
tags: json, property-order, transport
rationale: The graph, JSON text, and expected nested key sequences are handwritten in the ordered JSON fixture inventory.

=== json.accept.unusual-authored-keys ===
claim: Structural JSON preserves keys whose authored-HSON canonical spelling uses single-quoted names.
classification: structural-json-transport
ingress format: graph
escaped input: "{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\":x\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"colon\"]}]}]},{\"$_tag\":\"a.b\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"dot\"]}]}]},{\"$_tag\":\"\",\"$_content\":[{\"$_tag\":\"_hson_obj\",\"$_content\":[{\"$_tag\":\"_hson_str\",\"$_content\":[\"empty\"]}]}]}]}"
verbatim input: —
semantic shape: object
slot: object-property-order
variation: unusual-authored-keys
defect: —
disposition: accept
hand-authored expected graph:
{
  "$_content": [
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "colon"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": ":x"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "dot"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": "a.b"
    },
    {
      "$_content": [
        {
          "$_content": [
            {
              "$_content": [
                "empty"
              ],
              "$_tag": "_hson_str"
            }
          ],
          "$_tag": "_hson_obj"
        }
      ],
      "$_tag": ""
    }
  ],
  "$_tag": "_hson_obj"
}
exact HSON output: —
exact JSON output: "{\n  \":x\": \"colon\",\n  \"a.b\": \"dot\",\n  \"\": \"empty\"\n}"
exact HTML output: —
exact diagnostic output: —
expected key sequences:
[
  [
    ":x",
    "a.b",
    ""
  ]
]
expected string leaves: —
negative-zero paths: —
structured rejection: —
origin: certified-corpus-design
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: json, property-order, quoted-name, transport
rationale: The expected order is authored directly rather than recovered through JavaScript object enumeration.

=== json.reject.duplicate.adjacent ===
claim: Adjacent duplicate decoded keys reject before overwrite.
classification: structural-json-transport
ingress format: json-text
escaped input: "\"{\\\"x\\\":1,\\\"x\\\":2}\""
verbatim input: —
semantic shape: json-object
slot: property-key
variation: —
defect: adjacent
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_JSON_DUPLICATE_PROPERTY",
  "operation": "parse-json",
  "path": "$[\"x\"]",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 2,
        "index": 1,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 8,
    "index": 7,
    "line": 1
  },
  "stage": "parsing"
}
source evidence:
{
  "column": 8,
  "index": 7,
  "line": 1
}
path evidence: $["x"]
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 2,
      "index": 1,
      "line": 1
    }
  }
]
origin: structural-json-duplicate-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/json-ingress.acceptance.mts#duplicate-property-keys"
]
declared source reuse: —
tags: json, duplicate, rejection, structured-evidence
rationale: Adjacent duplicate decoded keys reject before overwrite.

=== json.reject.duplicate.array-contained ===
claim: Duplicate keys inside an array-contained object report the array path.
classification: structural-json-transport
ingress format: json-text
escaped input: "\"[{\\\"x\\\":1,\\\"x\\\":2}]\""
verbatim input: —
semantic shape: json-object
slot: property-key
variation: —
defect: array-contained
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_JSON_DUPLICATE_PROPERTY",
  "operation": "parse-json",
  "path": "$[0][\"x\"]",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 3,
        "index": 2,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 9,
    "index": 8,
    "line": 1
  },
  "stage": "parsing"
}
source evidence:
{
  "column": 9,
  "index": 8,
  "line": 1
}
path evidence: $[0]["x"]
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 3,
      "index": 2,
      "line": 1
    }
  }
]
origin: structural-json-duplicate-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/json-ingress.acceptance.mts#duplicate-property-keys"
]
declared source reuse: —
tags: json, duplicate, rejection, structured-evidence
rationale: Duplicate keys inside an array-contained object report the array path.

=== json.reject.duplicate.decoded-equivalent ===
claim: Literal and escaped spellings of the same decoded key are duplicates.
classification: structural-json-transport
ingress format: json-text
escaped input: "\"{\\\"x\\\":1,\\\"\\\\u0078\\\":2}\""
verbatim input: —
semantic shape: json-object
slot: property-key
variation: —
defect: decoded-equivalent
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_JSON_DUPLICATE_PROPERTY",
  "operation": "parse-json",
  "path": "$[\"x\"]",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 2,
        "index": 1,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 8,
    "index": 7,
    "line": 1
  },
  "stage": "parsing"
}
source evidence:
{
  "column": 8,
  "index": 7,
  "line": 1
}
path evidence: $["x"]
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 2,
      "index": 1,
      "line": 1
    }
  }
]
origin: structural-json-duplicate-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/json-ingress.acceptance.mts#duplicate-property-keys"
]
declared source reuse: —
tags: json, duplicate, rejection, structured-evidence
rationale: Literal and escaped spellings of the same decoded key are duplicates.

=== json.reject.duplicate.multiline-evidence ===
claim: Multiline duplicate coordinates identify both declarations exactly.
classification: structural-json-transport
ingress format: json-text
escaped input: "\"{\\n \\\"x\\\":1,\\n \\\"x\\\":2\\n}\""
verbatim input: "{\n \"x\":1,\n \"x\":2\n}"
semantic shape: json-object
slot: property-key
variation: —
defect: multiline-evidence
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_JSON_DUPLICATE_PROPERTY",
  "operation": "parse-json",
  "path": "$[\"x\"]",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 2,
        "index": 3,
        "line": 2
      }
    }
  ],
  "source": {
    "column": 2,
    "index": 11,
    "line": 3
  },
  "stage": "parsing"
}
source evidence:
{
  "column": 2,
  "index": 11,
  "line": 3
}
path evidence: $["x"]
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 2,
      "index": 3,
      "line": 2
    }
  }
]
origin: structural-json-duplicate-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/json-ingress.acceptance.mts#duplicate-property-keys"
]
declared source reuse: —
tags: json, duplicate, rejection, structured-evidence
rationale: Multiline duplicate coordinates identify both declarations exactly.

=== json.reject.duplicate.nested ===
claim: Nested duplicate keys report their nested structural path.
classification: structural-json-transport
ingress format: json-text
escaped input: "\"{\\\"outer\\\":{\\\"x\\\":1,\\\"x\\\":2}}\""
verbatim input: —
semantic shape: json-object
slot: property-key
variation: —
defect: nested
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_JSON_DUPLICATE_PROPERTY",
  "operation": "parse-json",
  "path": "$[\"outer\"][\"x\"]",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 11,
        "index": 10,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 17,
    "index": 16,
    "line": 1
  },
  "stage": "parsing"
}
source evidence:
{
  "column": 17,
  "index": 16,
  "line": 1
}
path evidence: $["outer"]["x"]
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 11,
      "index": 10,
      "line": 1
    }
  }
]
origin: structural-json-duplicate-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/json-ingress.acceptance.mts#duplicate-property-keys"
]
declared source reuse: —
tags: json, duplicate, rejection, structured-evidence
rationale: Nested duplicate keys report their nested structural path.

=== json.reject.duplicate.order-coexistence ===
claim: Duplicate rejection remains primary when integer-like ordering is also present.
classification: structural-json-transport
ingress format: json-text
escaped input: "\"{\\\"10\\\":1,\\\"2\\\":2,\\\"10\\\":3}\""
verbatim input: —
semantic shape: json-object
slot: property-key
variation: —
defect: order-coexistence
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_JSON_DUPLICATE_PROPERTY",
  "operation": "parse-json",
  "path": "$[\"10\"]",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 2,
        "index": 1,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 15,
    "index": 14,
    "line": 1
  },
  "stage": "parsing"
}
source evidence:
{
  "column": 15,
  "index": 14,
  "line": 1
}
path evidence: $["10"]
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 2,
      "index": 1,
      "line": 1
    }
  }
]
origin: structural-json-duplicate-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/json-ingress.acceptance.mts#duplicate-property-keys"
]
declared source reuse: —
tags: json, duplicate, rejection, structured-evidence
rationale: Duplicate rejection remains primary when integer-like ordering is also present.

=== json.reject.duplicate.separated ===
claim: Separated duplicate decoded keys reject before overwrite.
classification: structural-json-transport
ingress format: json-text
escaped input: "\"{\\\"x\\\":1,\\\"y\\\":0,\\\"x\\\":2}\""
verbatim input: —
semantic shape: json-object
slot: property-key
variation: —
defect: separated
disposition: reject
hand-authored expected graph: —
exact HSON output: —
exact JSON output: —
exact HTML output: —
structured rejection:
{
  "code": "HSON_JSON_DUPLICATE_PROPERTY",
  "operation": "parse-json",
  "path": "$[\"x\"]",
  "related": [
    {
      "role": "first-declaration",
      "source": {
        "column": 2,
        "index": 1,
        "line": 1
      }
    }
  ],
  "source": {
    "column": 14,
    "index": 13,
    "line": 1
  },
  "stage": "parsing"
}
source evidence:
{
  "column": 14,
  "index": 13,
  "line": 1
}
path evidence: $["x"]
related evidence:
[
  {
    "role": "first-declaration",
    "source": {
      "column": 2,
      "index": 1,
      "line": 1
    }
  }
]
origin: structural-json-duplicate-contract
provenance: —
novelty: —
human-review priority: —
specialized-test reference:
[
  "tests/json-ingress.acceptance.mts#duplicate-property-keys"
]
declared source reuse: —
tags: json, duplicate, rejection, structured-evidence
rationale: Separated duplicate decoded keys reject before overwrite.

=== reference.coordinate-mechanics ===
claim: Exhaustive coordinate mechanics remain specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/hson-tokenizer.acceptance.mts#structured-identities\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: coordinate-mechanics
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/hson-tokenizer.acceptance.mts#structured-identities"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, diagnostics
rationale: Exhaustive coordinate mechanics remain specialized ownership.

=== reference.late-duplicate-performance ===
claim: The 12,000-property late duplicate remains a scale/performance regression.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/json-ingress.acceptance.mts#late-duplicate-12000\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: late-duplicate-performance
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/json-ingress.acceptance.mts#late-duplicate-12000"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, json, performance
rationale: The 12,000-property late duplicate remains a scale/performance regression.

=== reference.malformed-graph-admission ===
claim: General malformed graph admission remains specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/hson-root-boundary.acceptance.mts#malformed-graph-admission\",\"tests/hson-structural-mode.acceptance.mts#canonical-invariant-admission\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: malformed-graph-admission
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/hson-root-boundary.acceptance.mts#malformed-graph-admission",
  "tests/hson-structural-mode.acceptance.mts#canonical-invariant-admission"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, graph-ingress, invariants
rationale: General malformed graph admission remains specialized ownership.

=== reference.oracle-self-tests ===
claim: Transform-oracle implementation self-tests remain specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/transform-oracle.acceptance.mts\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: oracle-self-tests
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/transform-oracle.acceptance.mts"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, oracle, diagnostics
rationale: Transform-oracle implementation self-tests remain specialized ownership.

=== reference.ordered-json-parser ===
claim: Low-level ordered JSON parser mechanics remain specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/json-ingress.acceptance.mts#structural-json-order\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: ordered-json-parser
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/json-ingress.acceptance.mts#structural-json-order"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, json
rationale: Low-level ordered JSON parser mechanics remain specialized ownership.

=== reference.quid-mechanics ===
claim: QUID ingress and egress mechanics remain specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/hson-node-quid-ingress.acceptance.mts\",\"tests/hson-node-quid-egress.acceptance.mts\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: quid-mechanics
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/hson-node-quid-ingress.acceptance.mts",
  "tests/hson-node-quid-egress.acceptance.mts"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, quid
rationale: QUID ingress and egress mechanics remain specialized ownership.

=== reference.runtime-wiring ===
claim: Worker and browser wiring remain specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/transform-worker.acceptance.mts\",\"hson-demo2/tests/browser/parse.spec.ts\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: runtime-wiring
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/transform-worker.acceptance.mts",
  "hson-demo2/tests/browser/parse.spec.ts"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, worker, browser
rationale: Worker and browser wiring remain specialized ownership.

=== reference.serializer-options ===
claim: Serializer noBreak/noQuid option matrices remain specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/hson-serializer.acceptance.mts#option-matrix\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: serializer-options
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/hson-serializer.acceptance.mts#option-matrix"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, serializer
rationale: Serializer noBreak/noQuid option matrices remain specialized ownership.

=== reference.tokenizer-token-arrays ===
claim: Tokenizer token-array shape remains specialized ownership.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"tests/hson-tokenizer.acceptance.mts#token-summary\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: tokenizer-token-arrays
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "tests/hson-tokenizer.acceptance.mts#token-summary"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, tokenizer
rationale: Tokenizer token-array shape remains specialized ownership.

=== reference.wikipedia-legacy-closure ===
claim: The Wikipedia real-world closure remains in its legacy HTML suite and is not an authored-HSON semantic case.
classification: specialized-test-cross-reference
ingress format: reference
escaped input: "[\"transform/legacy/html::html__largeFormat.html_wikipedia\"]"
verbatim input: —
semantic shape: specialized-suite
slot: cross-reference
variation: wikipedia-legacy-closure
defect: —
disposition: reference
hand-authored expected graph: —
structured rejection: —
specialized-test references:
[
  "transform/legacy/html::html__largeFormat.html_wikipedia"
]
origin: specialized-fixture-ownership
provenance: —
novelty: —
human-review priority: —
specialized-test reference: —
declared source reuse: —
tags: specialized-test, legacy-html, performance, wikipedia
rationale: The Wikipedia real-world closure remains in its legacy HTML suite and is not an authored-HSON semantic case.
