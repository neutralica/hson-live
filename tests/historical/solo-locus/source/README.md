# Retired solo hosted Locus source

These files preserve the pre-Step-6E one-map hosted authority, persistence, bootstrap, and Echo implementation for historical inspection. They are excluded from the production TypeScript build and npm package. The supported hosted runtime is the fixed library-registry Locus with session projection. Local one-library LiveMap APIs are unaffected.
