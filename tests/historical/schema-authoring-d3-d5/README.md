# Historical D3/D5 authoring fixtures

These fixtures record the retired solo LiveMap construction and later `map.schema.use` workflow. They are excluded from active test compilation and are kept only with the historical authoring design notes. Current Schema admission uses named libraries in `hsonLiveMap.fromLibraries(...)`.
